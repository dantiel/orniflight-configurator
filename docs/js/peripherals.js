// Generated by CoffeeScript 1.12.7
var adjustBoxNameIfPeripheralWithModeID, isPeripheralSelected;

isPeripheralSelected = function(peripheralName) {
  var portIndex, serialPort;
  portIndex = 0;
  while (portIndex < SERIAL_CONFIG.ports.length) {
    serialPort = SERIAL_CONFIG.ports[portIndex];
    if (serialPort.functions.indexOf(peripheralName) >= 0) {
      return true;
    }
    portIndex++;
  }
  return false;
};

adjustBoxNameIfPeripheralWithModeID = function(modeId, defaultName) {
  if (isPeripheralSelected('RUNCAM_DEVICE_CONTROL')) {
    switch (modeId) {
      case 32:
        return i18n.getMessage('modeCameraWifi');
      case 33:
        return i18n.getMessage('modeCameraPower');
      case 34:
        return i18n.getMessage('modeCameraChangeMode');
      default:
        return defaultName;
    }
  }
  return defaultName;
};

'use strict';
