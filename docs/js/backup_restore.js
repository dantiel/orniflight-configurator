// Generated by CoffeeScript 1.12.7
var _typeof, configuration_backup, configuration_restore;

_typeof = function(o) {
  '@babel/helpers - typeof';
  _typeof = 'function' === typeof Symbol && 'symbol' === typeof Symbol.iterator ? (function(o) {
    return typeof o;
  }) : (function(o) {
    if (o && 'function' === typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype) {
      return 'symbol';
    } else {
      return typeof o;
    }
  });
  return _typeof(o);
};

configuration_backup = function(callback) {
  var activeProfile, configuration, fetch_specific_data, fetch_unique_data, profileSpecificData, save, select_profile, uniqueData, update_profile_specific_data_list, update_unique_data_list, version;
  activeProfile = null;
  version = CONFIGURATOR.version;
  update_profile_specific_data_list = function() {
    if (semver.gt(CONFIG.apiVersion, '1.12.0')) {
      profileSpecificData.push(MSPCodes.MSP_SERVO_MIX_RULES);
    }
    if (semver.gte(CONFIG.apiVersion, '1.15.0')) {
      profileSpecificData.push(MSPCodes.MSP_RC_DEADBAND);
    }
  };
  select_profile = function() {
    if (activeProfile > 0) {
      MSP.send_message(MSPCodes.MSP_SELECT_SETTING, [0], false, fetch_specific_data);
    } else {
      fetch_specific_data();
    }
  };
  fetch_specific_data = function() {
    var codeKey, fetch_specific_data_item, fetchingProfile;
    fetchingProfile = 0;
    codeKey = 0;
    fetch_specific_data_item = function() {
      if (fetchingProfile < CONFIG.numProfiles) {
        MSP.send_message(profileSpecificData[codeKey], false, false, function() {
          codeKey++;
          if (codeKey < profileSpecificData.length) {
            fetch_specific_data_item();
          } else {
            configuration.profiles.push({
              'PID': jQuery.extend(true, {}, PID),
              'PIDs': jQuery.extend(true, [], PIDs),
              'RC': jQuery.extend(true, {}, RC_tuning),
              'AccTrim': jQuery.extend(true, [], CONFIG.accelerometerTrims),
              'ServoConfig': jQuery.extend(true, [], SERVO_CONFIG),
              'ServoRules': jQuery.extend(true, [], SERVO_RULES),
              'ModeRanges': jQuery.extend(true, [], MODE_RANGES),
              'AdjustmentRanges': jQuery.extend(true, [], ADJUSTMENT_RANGES)
            });
            if (semver.gte(CONFIG.apiVersion, '1.15.0')) {
              configuration.profiles[fetchingProfile].RCdeadband = jQuery.extend(true, {}, RC_DEADBAND_CONFIG);
            }
            codeKey = 0;
            fetchingProfile++;
            MSP.send_message(MSPCodes.MSP_SELECT_SETTING, [fetchingProfile], false, fetch_specific_data_item);
          }
        });
      } else {
        MSP.send_message(MSPCodes.MSP_SELECT_SETTING, [activeProfile], false, fetch_unique_data);
      }
    };
    fetch_specific_data_item();
  };
  update_unique_data_list = function() {
    if (semver.gte(CONFIG.apiVersion, '1.8.0')) {
      uniqueData.push(MSPCodes.MSP_LOOP_TIME);
      uniqueData.push(MSPCodes.MSP_ARMING_CONFIG);
    }
    if (semver.gte(CONFIG.apiVersion, '1.14.0')) {
      uniqueData.push(MSPCodes.MSP_MOTOR_3D_CONFIG);
    }
    if (semver.gte(CONFIG.apiVersion, '1.15.0')) {
      uniqueData.push(MSPCodes.MSP_SENSOR_ALIGNMENT);
      uniqueData.push(MSPCodes.MSP_RX_CONFIG);
      uniqueData.push(MSPCodes.MSP_FAILSAFE_CONFIG);
      uniqueData.push(MSPCodes.MSP_RXFAIL_CONFIG);
    }
    if (semver.gte(CONFIG.apiVersion, '1.19.0')) {
      uniqueData.push(MSPCodes.MSP_LED_STRIP_MODECOLOR);
    }
    if (semver.gte(CONFIG.apiVersion, '1.33.0')) {
      uniqueData.push(MSPCodes.MSP_MOTOR_CONFIG);
      uniqueData.push(MSPCodes.MSP_RSSI_CONFIG);
      uniqueData.push(MSPCodes.MSP_GPS_CONFIG);
      uniqueData.push(MSPCodes.MSP_COMPASS_CONFIG);
      uniqueData.push(MSPCodes.MSP_FEATURE_CONFIG);
    }
    if (semver.gte(CONFIG.apiVersion, '1.41.0')) {
      uniqueData.push(MSPCodes.MSP_MODE_RANGES_EXTRA);
    }
  };
  fetch_unique_data = function() {
    var codeKey, fetch_unique_data_item;
    codeKey = 0;
    fetch_unique_data_item = function() {
      if (codeKey < uniqueData.length) {
        MSP.send_message(uniqueData[codeKey], false, false, function() {
          codeKey++;
          fetch_unique_data_item();
        });
      } else {
        configuration.RCMAP = jQuery.extend(true, [], RC_MAP);
        configuration.SERIAL_CONFIG = jQuery.extend(true, {}, SERIAL_CONFIG);
        configuration.LED_STRIP = jQuery.extend(true, [], LED_STRIP);
        configuration.LED_COLORS = jQuery.extend(true, [], LED_COLORS);
        configuration.BOARD_ALIGNMENT_CONFIG = jQuery.extend(true, {}, BOARD_ALIGNMENT_CONFIG);
        configuration.CRAFT_NAME = CONFIG.name;
        configuration.DISPLAY_NAME = CONFIG.displayName;
        configuration.MIXER_CONFIG = jQuery.extend(true, {}, MIXER_CONFIG);
        configuration.SENSOR_CONFIG = jQuery.extend(true, {}, SENSOR_CONFIG);
        configuration.PID_ADVANCED_CONFIG = jQuery.extend(true, {}, PID_ADVANCED_CONFIG);
        if (semver.gte(CONFIG.apiVersion, '1.19.0')) {
          configuration.LED_MODE_COLORS = jQuery.extend(true, [], LED_MODE_COLORS);
        }
        if (semver.gte(CONFIG.apiVersion, '1.8.0')) {
          configuration.FC_CONFIG = jQuery.extend(true, {}, FC_CONFIG);
          configuration.ARMING_CONFIG = jQuery.extend(true, {}, ARMING_CONFIG);
        }
        if (semver.gte(CONFIG.apiVersion, '1.14.0')) {
          configuration.MOTOR_3D_CONFIG = jQuery.extend(true, {}, MOTOR_3D_CONFIG);
        }
        if (semver.gte(CONFIG.apiVersion, '1.15.0')) {
          configuration.SENSOR_ALIGNMENT = jQuery.extend(true, {}, SENSOR_ALIGNMENT);
          configuration.RX_CONFIG = jQuery.extend(true, {}, RX_CONFIG);
          configuration.FAILSAFE_CONFIG = jQuery.extend(true, {}, FAILSAFE_CONFIG);
          configuration.RXFAIL_CONFIG = jQuery.extend(true, [], RXFAIL_CONFIG);
        }
        if (semver.gte(CONFIG.apiVersion, '1.33.0')) {
          configuration.RSSI_CONFIG = jQuery.extend(true, {}, RSSI_CONFIG);
          configuration.FEATURE_CONFIG = jQuery.extend(true, {}, FEATURE_CONFIG);
          configuration.MOTOR_CONFIG = jQuery.extend(true, {}, MOTOR_CONFIG);
          configuration.GPS_CONFIG = jQuery.extend(true, {}, GPS_CONFIG);
          configuration.COMPASS_CONFIG = jQuery.extend(true, {}, COMPASS_CONFIG);
        }
        if (semver.gte(CONFIG.apiVersion, '1.36.0')) {
          configuration.BEEPER_CONFIG = jQuery.extend(true, {}, BEEPER_CONFIG);
        }
        if (semver.gte(CONFIG.apiVersion, '1.41.0')) {
          configuration.MODE_RANGES_EXTRA = jQuery.extend(true, [], MODE_RANGES_EXTRA);
        }
        save();
      }
    };
    if (GUI.configuration_loaded === true) {
      return fetch_unique_data_item();
    }
    MSP.promise(MSPCodes.MSP_ADVANCED_CONFIG).then(function() {
      return MSP.promise(MSPCodes.MSP_SENSOR_CONFIG);
    }).then(function() {
      return MSP.promise(MSPCodes.MSP_NAME);
    }).then(function() {
      return MSP.promise(MSPCodes.MSP_BOARD_ALIGNMENT_CONFIG);
    }).then(function() {
      return MSP.promise(MSPCodes.MSP_MIXER_CONFIG);
    }).then(function() {
      return MSP.promise(MSPCodes.MSP_BEEPER_CONFIG);
    }).then(function() {
      return fetch_unique_data_item();
    });
  };
  save = function() {
    var accepts, chosenFileEntry, filename, fs, input, prefix, serialized_config_object, suffix;
    prefix = 'backup';
    suffix = 'json';
    filename = generateFilename(prefix, suffix);
    serialized_config_object = JSON.stringify(configuration, null, '\u0009');
    if (GUI.Mode === GUI_Modes.NWJS) {
      fs = require('fs');
      input = document.createElement('input');
      input.type = 'file';
      input.nwsaveas = filename;
      input.onchange = function() {
        if (this.value) {
          fs.writeFileSync(this.value, serialized_config_object);
          console.log('Backup file path: ' + this.value);
          console.log('Write SUCCESSFUL');
          if (callback) {
            callback();
          }
        }
      };
      input.click();
      return;
    }
    chosenFileEntry = null;
    accepts = [
      {
        description: suffix.toUpperCase() + ' files',
        extensions: [suffix]
      }
    ];
    chrome.fileSystem.chooseEntry({
      type: 'saveFile',
      suggestedName: filename,
      accepts: accepts
    }, function(fileEntry) {
      if (chrome.runtime.lastError) {
        console.error(chrome.runtime.lastError.message);
        return;
      }
      if (!fileEntry) {
        console.log('No file selected, backup aborted.');
        return;
      }
      chosenFileEntry = fileEntry;
      chrome.fileSystem.getDisplayPath(chosenFileEntry, function(path) {
        console.log('Backup file path: ' + path);
      });
      chrome.fileSystem.getWritableEntry(chosenFileEntry, function(fileEntryWritable) {
        chrome.fileSystem.isWritableEntry(fileEntryWritable, function(isWritable) {
          var blob;
          if (isWritable) {
            chosenFileEntry = fileEntryWritable;
            blob = new Blob([serialized_config_object], {
              type: 'text/plain'
            });
            chosenFileEntry.createWriter((function(writer) {
              var truncated;
              writer.onerror = function(e) {
                console.error(e);
              };
              truncated = false;
              writer.onwriteend = function() {
                if (!truncated) {
                  truncated = true;
                  writer.truncate(blob.size);
                  return;
                }
                console.log('Write SUCCESSFUL');
                if (callback) {
                  callback();
                }
              };
              writer.write(blob);
            }), function(e) {
              console.error(e);
            });
          } else {
            console.log('File appears to be read only, sorry.');
          }
        });
      });
    });
  };
  if (version.indexOf('.') === -1) {
    version = version + '.0.0';
  }
  configuration = {
    'generatedBy': version,
    'apiVersion': CONFIG.apiVersion,
    'profiles': []
  };
  profileSpecificData = [MSPCodes.MSP_PID_CONTROLLER, MSPCodes.MSP_PID, MSPCodes.MSP_RC_TUNING, MSPCodes.MSP_ACC_TRIM, MSPCodes.MSP_SERVO_CONFIGURATIONS, MSPCodes.MSP_MODE_RANGES, MSPCodes.MSP_ADJUSTMENT_RANGES];
  update_profile_specific_data_list();
  MSP.send_message(MSPCodes.MSP_STATUS, false, false, function() {
    activeProfile = CONFIG.profile;
    select_profile();
  });
  uniqueData = [MSPCodes.MSP_RX_MAP, MSPCodes.MSP_CF_SERIAL_CONFIG, MSPCodes.MSP_LED_STRIP_CONFIG, MSPCodes.MSP_LED_COLORS];
  update_unique_data_list();
};

configuration_restore = function(callback) {
  var accepts, chosenFileEntry, compareVersions, configuration_upload, fs, input, migrate, process_configuration;
  process_configuration = function(configuration) {
    var features;
    if (typeof configuration.generatedBy !== 'undefined' && compareVersions(configuration.generatedBy, CONFIGURATOR.backupFileMinVersionAccepted)) {
      if (!compareVersions(configuration.generatedBy, '1.14.0') && !migrate(configuration)) {
        GUI.log(i18n.getMessage('backupFileUnmigratable'));
        return;
      }
      if (configuration.FEATURE_CONFIG && configuration.FEATURE_CONFIG.features && configuration.FEATURE_CONFIG.features._featureMask) {
        features = new Features(CONFIG);
        features.setMask(configuration.FEATURE_CONFIG.features._featureMask);
        configuration.FEATURE_CONFIG.features = features;
      }
      analytics.sendEvent(analytics.EVENT_CATEGORIES.FLIGHT_CONTROLLER, 'Restore');
      configuration_upload(configuration, callback);
    } else {
      GUI.log(i18n.getMessage('backupFileIncompatible'));
    }
  };
  compareVersions = function(generated, required) {
    if (generated === void 0) {
      return false;
    }
    return semver.gte(generated, required);
  };
  migrate = function(configuration) {
    var profileIndex;
    var profileIndex;
    var profileIndex;
    var i;
    var RC, appliedMigrationsCount, fixed_led_strip, i, index, migratedVersion, newPidControllerIndex, newPort, oldPort, portIndex, ports, profileIndex, rxfailChannel, servoConfig;
    appliedMigrationsCount = 0;
    migratedVersion = configuration.generatedBy;
    GUI.log(i18n.getMessage('configMigrationFrom', [migratedVersion]));
    if (!compareVersions(migratedVersion, '0.59.1')) {
      configuration.RSSI_CONFIG.channel = configuration.MISC.rssi_aux_channel;
      configuration.MISC.rssi_aux_channel = void 0;
      migratedVersion = '0.59.1';
      GUI.log(i18n.getMessage('configMigratedTo', [migratedVersion]));
      appliedMigrationsCount++;
    }
    if (!compareVersions(migratedVersion, '0.60.1')) {
      if (!configuration.LED_STRIP) {
        configuration.LED_STRIP = [];
      }
      migratedVersion = '0.60.1';
      GUI.log(i18n.getMessage('configMigratedTo', [migratedVersion]));
      appliedMigrationsCount++;
    }
    if (!compareVersions(migratedVersion, '0.61.0')) {
      if (!configuration.PIDs && configuration.PID) {
        configuration.PIDs = configuration.PID;
        configuration.PID = {
          controller: 0
        };
      }
      migratedVersion = '0.61.0';
      GUI.log(i18n.getMessage('configMigratedTo', [migratedVersion]));
      appliedMigrationsCount++;
    }
    if (!compareVersions(migratedVersion, '0.63.0')) {
      if (_typeof(configuration.LED_STRIP) === 'object') {
        fixed_led_strip = [];
        index = 0;
        while (configuration.LED_STRIP[index]) {
          fixed_led_strip.push(configuration.LED_STRIP[index++]);
        }
        configuration.LED_STRIP = fixed_led_strip;
      }
      profileIndex = 0;
      while (profileIndex < 3) {
        RC = configuration.profiles[profileIndex].RC;
        if (!RC.dynamic_THR_breakpoint) {
          RC.dynamic_THR_breakpoint = 1500;
        }
        RC.roll_rate = RC.roll_pitch_rate;
        RC.pitch_rate = RC.roll_pitch_rate;
        profileIndex++;
      }
      migratedVersion = '0.63.0';
      GUI.log(i18n.getMessage('configMigratedTo', [migratedVersion]));
      appliedMigrationsCount++;
    }
    if (configuration.apiVersion === void 0) {
      configuration.apiVersion = '1.0.0';
    }
    if (!semver.parse(configuration.apiVersion)) {
      configuration.apiVersion += '.0';
      if (!semver.parse(configuration.apiVersion)) {
        return false;
      }
    }
    if (compareVersions(migratedVersion, '0.63.0') && !compareVersions(configuration.apiVersion, '1.7.0')) {
      ports = [];
      portIndex = 0;
      while (portIndex < configuration.SERIAL_CONFIG.ports.length) {
        oldPort = configuration.SERIAL_CONFIG.ports[portIndex];
        newPort = {
          identifier: oldPort.identifier,
          functions: [],
          msp_baudrate: String(configuration.SERIAL_CONFIG.mspBaudRate),
          gps_baudrate: String(configuration.SERIAL_CONFIG.gpsBaudRate),
          telemetry_baudrate: 'AUTO',
          blackbox_baudrate: '115200'
        };
        switch (oldPort.scenario) {
          case 1:
          case 5:
          case 8:
            newPort.functions.push('MSP');
            break;
          case 2:
            newPort.functions.push('GPS');
            break;
          case 3:
            newPort.functions.push('RX_SERIAL');
            break;
          case 10:
            newPort.functions.push('BLACKBOX');
            break;
          case 11:
            newPort.functions.push('MSP');
            newPort.functions.push('BLACKBOX');
        }
        ports.push(newPort);
        portIndex++;
      }
      configuration.SERIAL_CONFIG = {
        ports: ports
      };
      GUI.log(i18n.getMessage('configMigratedTo', [migratedVersion]));
      appliedMigrationsCount++;
    }
    if (compareVersions(migratedVersion, '0.63.0') && !compareVersions(configuration.apiVersion, '1.8.0')) {
      if (configuration.FC_CONFIG === void 0) {
        configuration.FC_CONFIG = {
          loopTime: 3500
        };
      }
      if (configuration.ARMING_CONFIG === void 0) {
        configuration.ARMING_CONFIG = {
          auto_disarm_delay: 5,
          disarm_kill_switch: 1
        };
      }
      GUI.log(i18n.getMessage('configMigratedTo', [migratedVersion]));
      appliedMigrationsCount++;
    }
    if (compareVersions(migratedVersion, '0.63.0')) {
      if (configuration.FC_CONFIG.loopTime === 0) {
        configuration.FC_CONFIG.loopTime = 3500;
      }
    }
    if (semver.lt(migratedVersion, '0.66.0')) {
      profileIndex = 0;
      while (profileIndex < configuration.profiles.length) {
        if (semver.eq(configuration.apiVersion, '1.10.0')) {
          while (configuration.profiles[profileIndex].ServoConfig.length > 8) {
            configuration.profiles[profileIndex].ServoConfig.pop();
          }
        }
        i = 0;
        while (i < configuration.profiles[profileIndex].ServoConfig.length) {
          servoConfig = profiles[profileIndex].ServoConfig;
          servoConfig[i].angleAtMin = 45;
          servoConfig[i].angleAtMax = 45;
          servoConfig[i].reversedInputSources = 0;
          if (servoConfig[i].rate < -100 || servoConfig[i].rate > 100) {
            servoConfig[i].rate = 0;
          }
          i++;
        }
        configuration.profiles[profileIndex].ServoRules = [];
        profileIndex++;
      }
      migratedVersion = '0.66.0';
      GUI.log(i18n.getMessage('configMigratedTo', [migratedVersion]));
      appliedMigrationsCount++;
    }
    if (semver.lt(configuration.apiVersion, '1.14.0') && semver.gte(CONFIG.apiVersion, '1.14.0')) {
      profileIndex = 0;
      while (profileIndex < configuration.profiles.length) {
        newPidControllerIndex = configuration.profiles[profileIndex].PID.controller;
        switch (newPidControllerIndex) {
          case 3:
          case 4:
          case 5:
            newPidControllerIndex = 0;
        }
        configuration.profiles[profileIndex].PID.controller = newPidControllerIndex;
        profileIndex++;
      }
      GUI.log(i18n.getMessage('configMigratedTo', [migratedVersion]));
      appliedMigrationsCount++;
    }
    if (compareVersions(migratedVersion, '0.66.0') && !compareVersions(configuration.apiVersion, '1.14.0')) {
      if (configuration.MOTOR_3D_CONFIG === void 0) {
        configuration.MOTOR_3D_CONFIG = {
          deadband3d_low: 1406,
          deadband3d_high: 1514,
          neutral: 1460,
          deadband3d_throttle: 50
        };
      }
      GUI.log(i18n.getMessage('configMigratedTo', [migratedVersion]));
      appliedMigrationsCount++;
    }
    if (compareVersions(migratedVersion, '0.66.0') && !compareVersions(configuration.apiVersion, '1.15.0')) {
      profileIndex = 0;
      while (profileIndex < configuration.profiles.length) {
        if (configuration.profiles[profileIndex].RCdeadband === void 0) {
          configuration.profiles[profileIndex].RCdeadband = {
            deadband: 0,
            yaw_deadband: 0,
            alt_hold_deadband: 40
          };
        }
        profileIndex++;
      }
      if (configuration.SENSOR_ALIGNMENT === void 0) {
        configuration.SENSOR_ALIGNMENT = {
          align_gyro: 0,
          align_acc: 0,
          align_mag: 0
        };
      }
      if (configuration.RX_CONFIG === void 0) {
        configuration.RX_CONFIG = {
          serialrx_provider: 0,
          spektrum_sat_bind: 0,
          stick_center: 1500,
          stick_min: 1100,
          stick_max: 1900,
          rx_min_usec: 885,
          rx_max_usec: 2115
        };
      }
      if (configuration.FAILSAFE_CONFIG === void 0) {
        configuration.FAILSAFE_CONFIG = {
          failsafe_delay: 10,
          failsafe_off_delay: 200,
          failsafe_throttle: 1000,
          failsafe_switch_mode: 0,
          failsafe_throttle_low_delay: 100,
          failsafe_procedure: 0
        };
      }
      if (configuration.RXFAIL_CONFIG === void 0) {
        configuration.RXFAIL_CONFIG = [
          {
            mode: 0,
            value: 1500
          }, {
            mode: 0,
            value: 1500
          }, {
            mode: 0,
            value: 1500
          }, {
            mode: 0,
            value: 875
          }
        ];
        i = 0;
        while (i < 14) {
          rxfailChannel = {
            mode: 1,
            value: 1500
          };
          configuration.RXFAIL_CONFIG.push(rxfailChannel);
          i++;
        }
      }
      GUI.log(i18n.getMessage('configMigratedTo', [migratedVersion]));
      appliedMigrationsCount++;
    }
    if (compareVersions(migratedVersion, '1.2.0')) {
      if (GPS_CONFIG.ublox_sbas < 0) {
        GPS_CONFIG.ublox_sbas = 0;
      }
      migratedVersion = '1.2.0';
      GUI.log(i18n.getMessage('configMigratedTo', [migratedVersion]));
      appliedMigrationsCount++;
    }
    if (compareVersions(migratedVersion, '1.3.1')) {
      if (!configuration.LED_COLORS) {
        configuration.LED_COLORS = [];
      }
      if (!configuration.LED_MODE_COLORS) {
        configuration.LED_MODE_COLORS = [];
      }
      migratedVersion = '1.3.1';
      GUI.log(i18n.getMessage('configMigratedTo', [migratedVersion]));
      appliedMigrationsCount++;
    }
    if (appliedMigrationsCount > 0) {
      GUI.log(i18n.getMessage('configMigrationSuccessful', [appliedMigrationsCount]));
    }
    return true;
  };
  configuration_upload = function(configuration, callback) {
    var upload;
    upload = function() {
      var activeProfile, numProfiles, profileSpecificData, reboot, reinitialize, save_to_eeprom, select_profile, send_led_strip_colors, send_led_strip_config, send_led_strip_mode_colors, send_rxfail_config, upload_specific_data, upload_unique_data;
      activeProfile = null;
      numProfiles = CONFIG.numProfiles;
      select_profile = function() {
        if (activeProfile > 0) {
          MSP.send_message(MSPCodes.MSP_SELECT_SETTING, [0], false, upload_specific_data);
        } else {
          upload_specific_data();
        }
      };
      upload_specific_data = function() {
        var codeKey, load_objects, savingProfile, upload_adjustment_ranges, upload_mode_ranges, upload_servo_configuration, upload_using_specific_commands;
        savingProfile = 0;
        codeKey = 0;
        load_objects = function(profile) {
          var ADJUSTMENT_RANGES, MODE_RANGES, PID, PIDs, RC_DEADBAND_CONFIG, RC_tuning, SERVO_CONFIG, SERVO_RULES;
          PID = configuration.profiles[profile].PID;
          PIDs = configuration.profiles[profile].PIDs;
          RC_tuning = configuration.profiles[profile].RC;
          CONFIG.accelerometerTrims = configuration.profiles[profile].AccTrim;
          SERVO_CONFIG = configuration.profiles[profile].ServoConfig;
          SERVO_RULES = configuration.profiles[profile].ServoRules;
          MODE_RANGES = configuration.profiles[profile].ModeRanges;
          ADJUSTMENT_RANGES = configuration.profiles[profile].AdjustmentRanges;
          RC_DEADBAND_CONFIG = configuration.profiles[profile].RCdeadband;
        };
        upload_using_specific_commands = function() {
          MSP.send_message(profileSpecificData[codeKey], mspHelper.crunch(profileSpecificData[codeKey]), false, function() {
            codeKey++;
            if (codeKey < profileSpecificData.length) {
              upload_using_specific_commands();
            } else {
              codeKey = 0;
              savingProfile++;
              if (savingProfile < numProfiles) {
                load_objects(savingProfile);
                MSP.send_message(MSPCodes.MSP_EEPROM_WRITE, false, false, function() {
                  MSP.send_message(MSPCodes.MSP_SELECT_SETTING, [savingProfile], false, upload_using_specific_commands);
                });
              } else {
                MSP.send_message(MSPCodes.MSP_EEPROM_WRITE, false, false, function() {
                  MSP.send_message(MSPCodes.MSP_SELECT_SETTING, [activeProfile], false, upload_unique_data);
                });
              }
            }
          });
        };
        upload_servo_configuration = function() {
          mspHelper.sendServoConfigurations(upload_mode_ranges);
        };
        upload_mode_ranges = function() {
          var MODE_RANGES_EXTRA, defaultModeRangeExtra, modeIndex;
          if (semver.gte(CONFIG.apiVersion, '1.41.0')) {
            if (configuration.MODE_RANGES_EXTRA === void 0) {
              MODE_RANGES_EXTRA = [];
              modeIndex = 0;
              while (modeIndex < MODE_RANGES.length) {
                defaultModeRangeExtra = {
                  modeId: MODE_RANGES[modeIndex].modeId,
                  modeLogic: 0,
                  linkedTo: 0
                };
                MODE_RANGES_EXTRA.push(defaultModeRangeExtra);
                modeIndex++;
              }
            } else {
              MODE_RANGES_EXTRA = configuration.MODE_RANGES_EXTRA;
            }
          }
          mspHelper.sendModeRanges(upload_adjustment_ranges);
        };
        upload_adjustment_ranges = function() {
          mspHelper.sendAdjustmentRanges(upload_using_specific_commands);
        };
        load_objects(0);
        upload_servo_configuration();
      };
      upload_unique_data = function() {
        var codeKey, load_objects, send_unique_data_item, uniqueData, update_unique_data_list;
        codeKey = 0;
        uniqueData = [MSPCodes.MSP_SET_RX_MAP, MSPCodes.MSP_SET_CF_SERIAL_CONFIG];
        update_unique_data_list = function() {
          uniqueData.push(MSPCodes.MSP_SET_NAME);
          uniqueData.push(MSPCodes.MSP_SET_SENSOR_CONFIG);
          uniqueData.push(MSPCodes.MSP_SET_MIXER_CONFIG);
          uniqueData.push(MSPCodes.MSP_SET_BEEPER_CONFIG);
          uniqueData.push(MSPCodes.MSP_SET_BOARD_ALIGNMENT_CONFIG);
          uniqueData.push(MSPCodes.MSP_SET_ADVANCED_CONFIG);
          if (semver.gte(CONFIG.apiVersion, '1.8.0')) {
            uniqueData.push(MSPCodes.MSP_SET_LOOP_TIME);
            uniqueData.push(MSPCodes.MSP_SET_ARMING_CONFIG);
          }
          if (semver.gte(CONFIG.apiVersion, '1.14.0')) {
            uniqueData.push(MSPCodes.MSP_SET_MOTOR_3D_CONFIG);
          }
          if (semver.gte(CONFIG.apiVersion, '1.15.0')) {
            uniqueData.push(MSPCodes.MSP_SET_SENSOR_ALIGNMENT);
            uniqueData.push(MSPCodes.MSP_SET_RX_CONFIG);
            uniqueData.push(MSPCodes.MSP_SET_FAILSAFE_CONFIG);
          }
          if (semver.gte(CONFIG.apiVersion, '1.33.0')) {
            uniqueData.push(MSPCodes.MSP_SET_FEATURE_CONFIG);
            uniqueData.push(MSPCodes.MSP_SET_MOTOR_CONFIG);
            uniqueData.push(MSPCodes.MSP_SET_GPS_CONFIG);
            uniqueData.push(MSPCodes.MSP_SET_COMPASS_CONFIG);
            uniqueData.push(MSPCodes.MSP_SET_RSSI_CONFIG);
          }
        };
        load_objects = function() {
          var ARMING_CONFIG, BOARD_ALIGNMENT_CONFIG, COMPASS_CONFIG, FAILSAFE_CONFIG, FC_CONFIG, FEATURE_CONFIG, GPS_CONFIG, LED_COLORS, LED_MODE_COLORS, LED_STRIP, MISC, MIXER_CONFIG, MOTOR_3D_CONFIG, MOTOR_CONFIG, PID_ADVANCED_CONFIG, RC_MAP, RSSI_CONFIG, RXFAIL_CONFIG, RX_CONFIG, SENSOR_ALIGNMENT, SENSOR_CONFIG, SERIAL_CONFIG;
          MISC = configuration.MISC;
          RC_MAP = configuration.RCMAP;
          SERIAL_CONFIG = configuration.SERIAL_CONFIG;
          LED_STRIP = configuration.LED_STRIP;
          LED_COLORS = configuration.LED_COLORS;
          LED_MODE_COLORS = configuration.LED_MODE_COLORS;
          ARMING_CONFIG = configuration.ARMING_CONFIG;
          FC_CONFIG = configuration.FC_CONFIG;
          MOTOR_3D_CONFIG = configuration.MOTOR_3D_CONFIG;
          SENSOR_ALIGNMENT = configuration.SENSOR_ALIGNMENT;
          RX_CONFIG = configuration.RX_CONFIG;
          FAILSAFE_CONFIG = configuration.FAILSAFE_CONFIG;
          RXFAIL_CONFIG = configuration.RXFAIL_CONFIG;
          FEATURE_CONFIG = configuration.FEATURE_CONFIG;
          MOTOR_CONFIG = configuration.MOTOR_CONFIG;
          GPS_CONFIG = configuration.GPS_CONFIG;
          COMPASS_CONFIG = configuration.COMPASS_CONFIG;
          RSSI_CONFIG = configuration.RSSI_CONFIG;
          BOARD_ALIGNMENT_CONFIG = configuration.BOARD_ALIGNMENT_CONFIG;
          CONFIG.name = configuration.CRAFT_NAME;
          CONFIG.displayName = configuration.DISPLAY_NAME;
          MIXER_CONFIG = configuration.MIXER_CONFIG;
          SENSOR_CONFIG = configuration.SENSOR_CONFIG;
          PID_ADVANCED_CONFIG = configuration.PID_ADVANCED_CONFIG;
          BEEPER_CONFIG.beepers = new Beepers(CONFIG);
          BEEPER_CONFIG.beepers.setMask(configuration.BEEPER_CONFIG.beepers._beeperMask);
          BEEPER_CONFIG.dshotBeaconTone = configuration.BEEPER_CONFIG.dshotBeaconTone;
          BEEPER_CONFIG.dshotBeaconConditions = new Beepers(CONFIG, ['RX_LOST', 'RX_SET']);
          BEEPER_CONFIG.dshotBeaconConditions.setMask(configuration.BEEPER_CONFIG.dshotBeaconConditions._beeperMask);
        };
        send_unique_data_item = function() {
          if (codeKey < uniqueData.length) {
            MSP.send_message(uniqueData[codeKey], mspHelper.crunch(uniqueData[codeKey]), false, function() {
              codeKey++;
              send_unique_data_item();
            });
          } else {
            send_led_strip_config();
          }
        };
        load_objects();
        update_unique_data_list();
        send_unique_data_item();
      };
      send_led_strip_config = function() {
        mspHelper.sendLedStripConfig(send_led_strip_colors);
      };
      send_led_strip_colors = function() {
        mspHelper.sendLedStripColors(send_led_strip_mode_colors);
      };
      send_led_strip_mode_colors = function() {
        if (semver.gte(CONFIG.apiVersion, '1.19.0')) {
          mspHelper.sendLedStripModeColors(send_rxfail_config);
        } else {
          send_rxfail_config();
        }
      };
      send_rxfail_config = function() {
        if (semver.gte(CONFIG.apiVersion, '1.15.0')) {
          mspHelper.sendRxFailConfig(save_to_eeprom);
        } else {
          save_to_eeprom();
        }
      };
      save_to_eeprom = function() {
        MSP.send_message(MSPCodes.MSP_EEPROM_WRITE, false, false, reboot);
      };
      reboot = function() {
        GUI.log(i18n.getMessage('eeprom_saved_ok'));
        GUI.tab_switch_cleanup(function() {
          MSP.send_message(MSPCodes.MSP_SET_REBOOT, false, false, reinitialize);
        });
      };
      reinitialize = function() {
        GUI.log(i18n.getMessage('deviceRebooting'));
        GUI.timeout_add('waiting_for_bootup', (function() {
          MSP.send_message(MSPCodes.MSP_STATUS, false, false, function() {
            GUI.log(i18n.getMessage('deviceReady'));
            if (callback) {
              callback();
            }
          });
        }), 1500);
      };
      if (configuration.profiles.length < numProfiles) {
        numProfiles = configuration.profiles.length;
      }
      profileSpecificData = [MSPCodes.MSP_SET_PID_CONTROLLER, MSPCodes.MSP_SET_PID, MSPCodes.MSP_SET_RC_TUNING, MSPCodes.MSP_SET_ACC_TRIM];
      if (semver.gte(CONFIG.apiVersion, '1.15.0')) {
        profileSpecificData.push(MSPCodes.MSP_SET_RC_DEADBAND);
      }
      MSP.send_message(MSPCodes.MSP_STATUS, false, false, function() {
        activeProfile = CONFIG.profile;
        select_profile();
      });
    };
    upload();
  };
  if (GUI.Mode === GUI_Modes.NWJS) {
    fs = require('fs');
    input = document.createElement('input');
    input.type = 'file';
    input.accept = '.json';
    input.onchange = function() {
      var configuration, content, e;
      if (!this.value) {
        console.log('No file selected, restore aborted.');
        return;
      }
      content = fs.readFileSync(this.value, 'utf8');
      if (content.length > 1048576) {
        console.log('File limit (1 MB) exceeded, aborting');
        return;
      }
      console.log('Restore file path: ' + this.value);
      console.log('Read SUCCESSFUL');
      try {
        configuration = JSON.parse(content);
      } catch (error) {
        e = error;
        console.log('Data provided != valid JSON string, restore aborted.');
        return;
      }
      process_configuration(configuration);
    };
    input.click();
    return;
  }
  chosenFileEntry = null;
  accepts = [
    {
      description: 'JSON files',
      extensions: ['json']
    }
  ];
  chrome.fileSystem.chooseEntry({
    type: 'openFile',
    accepts: accepts
  }, function(fileEntry) {
    if (chrome.runtime.lastError) {
      console.error(chrome.runtime.lastError.message);
      return;
    }
    if (!fileEntry) {
      console.log('No file selected, restore aborted.');
      return;
    }
    chosenFileEntry = fileEntry;
    chrome.fileSystem.getDisplayPath(chosenFileEntry, function(path) {
      console.log('Restore file path: ' + path);
    });
    chosenFileEntry.file(function(file) {
      var reader;
      reader = new FileReader;
      reader.onprogress = function(e) {
        if (e.total > 1048576) {
          console.log('File limit (1 MB) exceeded, aborting');
          reader.abort();
        }
      };
      reader.onloadend = function(e) {
        var configuration;
        if (e.total !== 0 && e.total === e.loaded) {
          console.log('Read SUCCESSFUL');
          try {
            configuration = JSON.parse(e.target.result);
          } catch (error) {
            e = error;
            console.log('Data provided != valid JSON string, restore aborted.');
            return;
          }
          process_configuration(configuration);
        }
      };
      reader.readAsText(file);
    });
  });
};

'use strict';
