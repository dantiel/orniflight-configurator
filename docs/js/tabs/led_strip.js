// Generated by CoffeeScript 1.12.7
'use strict';
TABS.led_strip = {
  wireMode: false,
  directions: ['n', 'e', 's', 'w', 'u', 'd']
};

TABS.led_strip.initialize = function(callback, scrollPosition) {
  var HsvToColor, applyFunctionToSelectedLeds, areBlinkersActive, areModifiersActive, areOverlaysActive, buildUsedWireNumbers, clearModeColorSelection, drawColorBoxesInColorLedPoints, findLed, getModeColor, isVtxActive, isWarningActive, load_html, load_led_colors, load_led_config, load_led_mode_colors, process_html, selectedColorIndex, selectedModeColor, self, setBackgroundColor, setColorSliders, setModeBackgroundColor, setModeColor, setOptionalGroupsVisibility, unselectOverlay, unselectOverlays, updateBulkCmd, updateColors;
  self = this;
  selectedColorIndex = null;
  selectedModeColor = null;
  load_led_config = function() {
    MSP.send_message(MSPCodes.MSP_LED_STRIP_CONFIG, false, false, load_led_colors);
  };
  load_led_colors = function() {
    MSP.send_message(MSPCodes.MSP_LED_COLORS, false, false, load_led_mode_colors);
  };
  load_led_mode_colors = function() {
    if (semver.gte(CONFIG.apiVersion, '1.19.0')) {
      MSP.send_message(MSPCodes.MSP_LED_STRIP_MODECOLOR, false, false, load_html);
    } else {
      load_html();
    }
  };
  load_html = function() {
    $('#content').load('./tabs/led_strip.html', process_html);
  };
  buildUsedWireNumbers = function() {
    var usedWireNumbers;
    usedWireNumbers = [];
    $('.mainGrid .gPoint .wire').each(function() {
      var wireNumber;
      wireNumber = parseInt($(this).html());
      if (wireNumber >= 0) {
        usedWireNumbers.push(wireNumber);
      }
    });
    usedWireNumbers.sort(function(a, b) {
      return a - b;
    });
    return usedWireNumbers;
  };
  process_html = function() {
    var i;
    var AuxDir, AuxMode, i, ip, removeFunctionsAndDirections, theHTML, theHTMLlength, toggleSwitch;
    removeFunctionsAndDirections = function(element) {
      var classesToRemove;
      classesToRemove = [];
      TABS.led_strip.baseFuncs.forEach(function(letter) {
        classesToRemove.push('function-' + letter);
      });
      TABS.led_strip.overlays.forEach(function(letter) {
        classesToRemove.push('function-' + letter);
      });
      TABS.led_strip.directions.forEach(function(letter) {
        classesToRemove.push('dir-' + letter);
      });
      $(element).removeClass(classesToRemove.join(' '));
    };
    toggleSwitch = function(that, letter) {
      if ($(that).is(':checked')) {
        $('.ui-selected').find('.wire').each(function() {
          var p;
          if ($(this).text() !== '') {
            p = $(this).parent();
            TABS.led_strip.functions.forEach(function(f) {
              if (p.is('.function-' + f)) {
                switch (letter) {
                  case 't':
                  case 'o':
                  case 's':
                    if (areModifiersActive('function-' + f)) {
                      p.addClass('function-' + letter);
                    }
                    break;
                  case 'b':
                  case 'n':
                    if (areBlinkersActive('function-' + f)) {
                      p.addClass('function-' + letter);
                    }
                    break;
                  case 'i':
                    if (areOverlaysActive('function-' + f)) {
                      p.addClass('function-' + letter);
                    }
                    break;
                  case 'w':
                    if (areOverlaysActive('function-' + f)) {
                      if (isWarningActive('function-' + f)) {
                        p.addClass('function-' + letter);
                      }
                    }
                    break;
                  case 'v':
                    if (areOverlaysActive('function-' + f)) {
                      if (isVtxActive('function-' + f)) {
                        p.addClass('function-' + letter);
                      }
                    }
                }
              }
            });
          }
        });
      } else {
        $('.ui-selected').removeClass('function-' + letter);
      }
      return $(that).is(':checked');
    };
    i18n.localizePage();
    theHTML = [];
    theHTMLlength = 0;
    i = 0;
    while (i < 256) {
      if (semver.lte(CONFIG.apiVersion, '1.19.0')) {
        theHTML[theHTMLlength++] = '<div class="gPoint"><div class="indicators"><span class="north"></span><span class="south"></span><span class="west"></span><span class="east"></span><span class="up">U</span><span class="down">D</span></div><span class="wire"></span><span class="overlay-t"> </span><span class="overlay-s"> </span><span class="overlay-w"> </span><span class="overlay-i"> </span><span class="overlay-color"> </span></div>';
      } else if (semver.lt(CONFIG.apiVersion, '1.36.0')) {
        theHTML[theHTMLlength++] = '<div class="gPoint"><div class="indicators"><span class="north"></span><span class="south"></span><span class="west"></span><span class="east"></span><span class="up">U</span><span class="down">D</span></div><span class="wire"></span><span class="overlay-t"> </span><span class="overlay-o"> </span><span class="overlay-b"> </span><span class="overlay-n"> </span><span class="overlay-i"> </span><span class="overlay-w"> </span><span class="overlay-color"> </span></div>';
      } else {
        theHTML[theHTMLlength++] = '<div class="gPoint"><div class="indicators"><span class="north"></span><span class="south"></span><span class="west"></span><span class="east"></span><span class="up">U</span><span class="down">D</span></div><span class="wire"></span><span class="overlay-t"> </span><span class="overlay-o"> </span><span class="overlay-b"> </span><span class="overlay-v"> </span><span class="overlay-i"> </span><span class="overlay-w"> </span><span class="overlay-color"> </span></div>';
      }
      i++;
    }
    $('.mainGrid').html(theHTML.join(''));
    $('.tempOutput').click(function() {
      $(this).select();
    });
    if (semver.lte(CONFIG.apiVersion, '1.20.0')) {
      $('.auxSelect').hide();
      $('.labelSelect').show();
    } else {
      $('.auxSelect').show();
      $('.labelSelect').hide();
      AuxMode = 7;
      AuxDir = 0;
      $('.auxSelect').val(getModeColor(AuxMode, AuxDir));
      $('.auxSelect').on('change', function() {
        setModeColor(AuxMode, AuxDir, $('.auxSelect').val());
      });
    }
    if (semver.lt(CONFIG.apiVersion, '1.36.0')) {
      $('.vtxOverlay').hide();
      $('.landingBlinkOverlay').show();
    } else {
      $('.landingBlinkOverlay').css('visibility', 'hidden');
      $('.vtxOverlay').show();
    }
    $('.funcClear').click(function() {
      $('.gPoint').each(function() {
        if ($(this).is('.ui-selected')) {
          removeFunctionsAndDirections(this);
          $(this).find('.wire').html('');
        }
      });
      $('.controls button').removeClass('btnOn');
      updateBulkCmd();
    });
    $('.funcClearAll').click(function() {
      $('.gPoint').each(function() {
        removeFunctionsAndDirections(this);
      });
      $('.gPoint .wire').html('');
      updateBulkCmd();
      $('.controls button').removeClass('btnOn');
    });
    $('.directions').on('click', 'button', function() {
      var that;
      that = this;
      if ($('.ui-selected').length > 0) {
        TABS.led_strip.directions.forEach(function(letter) {
          if ($(that).is('.dir-' + letter)) {
            if ($(that).is('.btnOn')) {
              $(that).removeClass('btnOn');
              $('.ui-selected').removeClass('dir-' + letter);
            } else {
              $(that).addClass('btnOn');
              $('.ui-selected').addClass('dir-' + letter);
            }
          }
        });
        clearModeColorSelection();
        updateBulkCmd();
      }
    });
    $('.mode_colors').on('click', 'button', function() {
      var that;
      that = this;
      LED_MODE_COLORS.forEach(function(mc) {
        var className, colorIndex;
        if ($(that).is('.mode_color-' + mc.mode + '-' + mc.direction)) {
          if ($(that).is('.btnOn')) {
            $(that).removeClass('btnOn');
            $('.ui-selected').removeClass('mode_color-' + mc.mode + '-' + mc.direction);
            selectedModeColor = null;
          } else {
            $(that).addClass('btnOn');
            selectedModeColor = {
              mode: mc.mode,
              direction: mc.direction
            };
            colorIndex = 0;
            while (colorIndex < 16) {
              className = '.color-' + colorIndex;
              if (colorIndex === getModeColor(mc.mode, mc.direction)) {
                $(className).addClass('btnOn');
                selectedColorIndex = colorIndex;
                setColorSliders(colorIndex);
              } else {
                $(className).removeClass('btnOn');
              }
              colorIndex++;
            }
          }
        }
      });
      $('.mode_colors').each(function() {
        $(this).children().each(function() {
          if (!$(this).is($(that))) {
            if ($(this).is('.btnOn')) {
              $(this).removeClass('btnOn');
            }
          }
        });
      });
      updateBulkCmd();
    });
    ip = $('div.colorDefineSliders input');
    ip.eq(0).on('input change', function() {
      updateColors($(this).val(), 0);
    });
    ip.eq(1).on('input change', function() {
      updateColors($(this).val(), 1);
    });
    ip.eq(2).on('input change', function() {
      updateColors($(this).val(), 2);
    });
    i = 0;
    while (i < 3) {
      updateColors(ip.eq(i).val(), i);
      i++;
    }
    $('.colors').on('click', 'button', function(e) {
      var colorButtons, colorIndex, that;
      that = this;
      colorButtons = $(this).parent().find('button');
      colorIndex = 0;
      while (colorIndex < 16) {
        colorButtons.removeClass('btnOn');
        if (selectedModeColor === void 0) {
          $('.ui-selected').removeClass('color-' + colorIndex);
        }
        if ($(that).is('.color-' + colorIndex)) {
          selectedColorIndex = colorIndex;
          if (selectedModeColor === void 0) {
            $('.ui-selected').addClass('color-' + colorIndex);
          }
        }
        colorIndex++;
      }
      setColorSliders(selectedColorIndex);
      $(this).addClass('btnOn');
      if (selectedModeColor) {
        setModeColor(selectedModeColor.mode, selectedModeColor.direction, selectedColorIndex);
      }
      drawColorBoxesInColorLedPoints();
      $('.colors').children().each(function() {
        setBackgroundColor($(this));
      });
      $('.overlay-color').each(function() {
        setBackgroundColor($(this));
      });
      $('.mode_colors').each(function() {
        setModeBackgroundColor($(this));
      });
      $('.special_colors').each(function() {
        setModeBackgroundColor($(this));
      });
      updateBulkCmd();
    });
    $('.colors').on('dblclick', 'button', function() {
      $('.colorDefineSliders').css('left', $(this).position().left - ($('.colorDefineSliders').width() / 2) + $(this).width());
      $('.colorDefineSliders').css('top', $(this).position().top + 26);
      $('.colorDefineSliders').show();
    });
    $('.colors').children().on({
      mouseleave: function() {
        if (!$('.colorDefineSliders').is(':hover')) {
          $('.colorDefineSliders').hide();
        }
      }
    });
    $('.funcWire').click(function() {
      $(this).toggleClass('btnOn');
      TABS.led_strip.wireMode = $(this).hasClass('btnOn');
      $('.mainGrid').toggleClass('gridWire');
    });
    $('.funcWireClearSelect').click(function() {
      $('.ui-selected').each(function() {
        var thisWire;
        thisWire = $(this).find('.wire');
        if (thisWire.html() !== '') {
          thisWire.html('');
        }
        updateBulkCmd();
      });
    });
    $('.funcWireClear').click(function() {
      $('.gPoint .wire').html('');
      updateBulkCmd();
    });
    $('.mainGrid').selectable({
      filter: ' > div',
      stop: function() {
        var className, colorIndex, directionsInSelection, functionsInSelection, that, uiSelectedLast;
        functionsInSelection = [];
        directionsInSelection = [];
        clearModeColorSelection();
        that = void 0;
        $('.ui-selected').each(function() {
          var nextWireNumber;
          var nextWireNumber, usedWireNumbers;
          usedWireNumbers = buildUsedWireNumbers();
          nextWireNumber = 0;
          nextWireNumber = 0;
          while (nextWireNumber < usedWireNumbers.length) {
            if (usedWireNumbers[nextWireNumber] !== nextWireNumber) {
              break;
            }
            nextWireNumber++;
          }
          if (TABS.led_strip.wireMode) {
            if ($(this).find('.wire').html() === '' && nextWireNumber < LED_STRIP.length) {
              $(this).find('.wire').html(nextWireNumber);
            }
          }
          if ($(this).find('.wire').text() !== '') {
            that = this;
            TABS.led_strip.directions.forEach(function(letter) {
              var className;
              className = '.dir-' + letter;
              if ($(that).is(className)) {
                directionsInSelection.push(className);
              }
            });
            TABS.led_strip.baseFuncs.forEach(function(letter) {
              var className;
              className = '.function-' + letter;
              if ($(that).is(className)) {
                functionsInSelection.push(className);
              }
            });
            TABS.led_strip.overlays.forEach(function(letter) {
              var className;
              className = '.function-' + letter;
              if ($(that).is(className)) {
                functionsInSelection.push(className);
              }
            });
          }
        });
        uiSelectedLast = that;
        $('select.functionSelect').val('');
        TABS.led_strip.baseFuncs.forEach(function(letter) {
          var className;
          className = 'function-' + letter;
          if ($('select.functionSelect').is('.' + className)) {
            $('select.functionSelect').removeClass(className);
          }
        });
        selectedColorIndex = 0;
        if (uiSelectedLast) {
          colorIndex = 0;
          while (colorIndex < 16) {
            className = '.color-' + colorIndex;
            if ($(uiSelectedLast).is(className)) {
              $(className).addClass('btnOn');
              selectedColorIndex = colorIndex;
            } else {
              $(className).removeClass('btnOn');
            }
            colorIndex++;
          }
          TABS.led_strip.overlays.forEach(function(letter) {
            var feature_o, newVal;
            feature_o = $('.checkbox').find('input.function-' + letter);
            newVal = $(uiSelectedLast).is('.function-' + letter);
            if (feature_o.is(':checked') !== newVal) {
              feature_o.prop('checked', newVal);
              feature_o.change();
            }
          });
          TABS.led_strip.baseFuncs.forEach(function(letter) {
            if ($(uiSelectedLast).is('.function-' + letter)) {
              $('select.functionSelect').val('function-' + letter);
              $('select.functionSelect').addClass('function-' + letter);
            }
          });
        }
        updateBulkCmd();
        setColorSliders(selectedColorIndex);
        setOptionalGroupsVisibility();
        $('.directions button').removeClass('btnOn');
        directionsInSelection.forEach(function(direction_e) {
          $(direction_e).addClass('btnOn');
        });
      }
    });
    $('.functionSelect').on('change', function() {
      clearModeColorSelection();
      applyFunctionToSelectedLeds();
      drawColorBoxesInColorLedPoints();
      setOptionalGroupsVisibility();
      updateBulkCmd();
    });
    $('.modeSelect').on('change', function() {
      var mode, that;
      that = this;
      mode = Number($(that).val());
      $('.mode_colors').find('button').each(function() {
        var i;
        var j;
        i = 0;
        while (i < 6) {
          j = 0;
          while (j < 6) {
            if ($(this).hasClass('mode_color-' + i + '-' + j)) {
              $(this).removeClass('mode_color-' + i + '-' + j);
              $(this).addClass('mode_color-' + mode + '-' + j);
            }
            j++;
          }
          i++;
        }
      });
      $('.mode_colors').each(function() {
        setModeBackgroundColor($(this));
      });
    });
    $('.checkbox').change(function(e) {
      var that;
      if (e.originalEvent) {
        that = $(this).find('input');
        if ($('.ui-selected').length > 0) {
          TABS.led_strip.overlays.forEach(function(letter) {
            var cbb, cbn, ret;
            if ($(that).is('.function-' + letter)) {
              ret = toggleSwitch(that, letter);
              cbn = $('.checkbox .function-n');
              cbb = $('.checkbox .function-b');
              if (ret) {
                if (letter === 'b' && cbn.is(':checked')) {
                  cbn.prop('checked', false);
                  cbn.change();
                  toggleSwitch(cbn, 'n');
                } else if (letter === 'n' && cbb.is(':checked')) {
                  cbb.prop('checked', false);
                  cbb.change();
                  toggleSwitch(cbb, 'b');
                }
              }
            }
          });
          clearModeColorSelection();
          updateBulkCmd();
          setOptionalGroupsVisibility();
        }
      } else {

      }
    });
    $('.mainGrid').disableSelection();
    $('.gPoint').each(function() {
      var col, directionIndex, gridNumber, led, ledIndex, ledResult, modeIndex, row;
      gridNumber = $(this).index() + 1;
      row = Math.ceil(gridNumber / 16) - 1;
      col = gridNumber / 16 % 1 * 16 - 1;
      if (col < 0) {
        col = 15;
      }
      ledResult = findLed(col, row);
      if (!ledResult) {
        return;
      }
      ledIndex = ledResult.index;
      led = ledResult.led;
      if (led.functions[0] === 'c' && led.functions.length === 1 && led.directions.length === 0 && led.color === 0 && led.x === 0 && led.y === 0) {
        return;
      }
      $(this).find('.wire').html(ledIndex);
      modeIndex = 0;
      while (modeIndex < led.functions.length) {
        $(this).addClass('function-' + led.functions[modeIndex]);
        modeIndex++;
      }
      directionIndex = 0;
      while (directionIndex < led.directions.length) {
        $(this).addClass('dir-' + led.directions[directionIndex]);
        directionIndex++;
      }
      $(this).addClass('color-' + led.color);
    });
    $('a.save').click(function() {
      var save_to_eeprom, send_led_strip_colors, send_led_strip_mode_colors;
      send_led_strip_colors = function() {
        mspHelper.sendLedStripColors(send_led_strip_mode_colors);
      };
      send_led_strip_mode_colors = function() {
        if (semver.gte(CONFIG.apiVersion, '1.19.0')) {
          mspHelper.sendLedStripModeColors(save_to_eeprom);
        } else {
          save_to_eeprom();
        }
      };
      save_to_eeprom = function() {
        MSP.send_message(MSPCodes.MSP_EEPROM_WRITE, false, false, function() {
          GUI.log(i18n.getMessage('ledStripEepromSaved'));
        });
      };
      mspHelper.sendLedStripConfig(send_led_strip_colors);
    });
    $('.colorDefineSliders').hide();
    applyFunctionToSelectedLeds();
    drawColorBoxesInColorLedPoints();
    setOptionalGroupsVisibility();
    updateBulkCmd();
    GUI.content_ready(callback);
  };
  findLed = function(x, y) {
    var led, ledIndex;
    ledIndex = 0;
    while (ledIndex < LED_STRIP.length) {
      led = LED_STRIP[ledIndex];
      if (led.x === x && led.y === y) {
        return {
          index: ledIndex,
          led: led
        };
      }
      ledIndex++;
    }
    return void 0;
  };
  updateBulkCmd = function() {
    var LED_STRIP, defaultLed, i, ledStripLength, remaining, usedWireNumbers;
    ledStripLength = LED_STRIP.length;
    LED_STRIP = [];
    $('.gPoint').each(function() {
      var col, colorIndex, directions, functions, gridNumber, led, match, row, that, wireNumber;
      if ($(this).is('[class*="function"]')) {
        gridNumber = $(this).index() + 1;
        row = Math.ceil(gridNumber / 16) - 1;
        col = gridNumber / 16 % 1 * 16 - 1;
        if (col < 0) {
          col = 15;
        }
        wireNumber = $(this).find('.wire').html();
        functions = '';
        directions = '';
        colorIndex = 0;
        that = this;
        match = $(this).attr('class').match(/(^|\s)color-([0-9]+)(\s|$)/);
        if (match) {
          colorIndex = match[2];
        }
        TABS.led_strip.baseFuncs.forEach(function(letter) {
          if ($(that).is('.function-' + letter)) {
            functions += letter;
          }
        });
        TABS.led_strip.overlays.forEach(function(letter) {
          if ($(that).is('.function-' + letter)) {
            functions += letter;
          }
        });
        TABS.led_strip.directions.forEach(function(letter) {
          if ($(that).is('.dir-' + letter)) {
            directions += letter;
          }
        });
        if (wireNumber !== '') {
          led = {
            x: col,
            y: row,
            directions: directions,
            functions: functions,
            color: colorIndex
          };
          LED_STRIP[wireNumber] = led;
        }
      }
    });
    defaultLed = {
      x: 0,
      y: 0,
      directions: '',
      functions: ''
    };
    i = 0;
    while (i < ledStripLength) {
      if (LED_STRIP[i]) {
        i++;
        continue;
      }
      LED_STRIP[i] = defaultLed;
      i++;
    }
    usedWireNumbers = buildUsedWireNumbers();
    remaining = LED_STRIP.length - usedWireNumbers.length;
    $('.wires-remaining div').html(remaining);
  };
  setModeBackgroundColor = function(element) {
    if (semver.gte(CONFIG.apiVersion, '1.19.0')) {
      element.find('[class*="mode_color"]').each(function() {
        var d, m, match;
        m = 0;
        d = 0;
        match = $(this).attr('class').match(/(^|\s)mode_color-([0-9]+)-([0-9]+)(\s|$)/);
        if (match) {
          m = Number(match[2]);
          d = Number(match[3]);
          $(this).css('background-color', HsvToColor(LED_COLORS[getModeColor(m, d)]));
        }
      });
    }
  };
  setBackgroundColor = function(element) {
    var colorIndex, match;
    if (element.is('[class*="color"]')) {
      colorIndex = 0;
      match = element.attr('class').match(/(^|\s)color-([0-9]+)(\s|$)/);
      if (match) {
        colorIndex = match[2];
        element.css('background-color', HsvToColor(LED_COLORS[colorIndex]));
      }
    }
  };
  areModifiersActive = function(activeFunction) {
    switch (activeFunction) {
      case 'function-c':
      case 'function-a':
      case 'function-f':
        return true;
    }
    return false;
  };
  areOverlaysActive = function(activeFunction) {
    if (semver.lt(CONFIG.apiVersion, '1.20.0')) {
      switch (activeFunction) {
        case 'function-c':
        case 'function-a':
        case 'function-f':
        case 'function-g':
          return true;
      }
    } else {
      switch (activeFunction) {
        case '':
        case 'function-c':
        case 'function-a':
        case 'function-f':
        case 'function-s':
        case 'function-l':
        case 'function-r':
        case 'function-o':
        case 'function-g':
          return true;
      }
    }
    return false;
  };
  areBlinkersActive = function(activeFunction) {
    if (semver.gte(CONFIG.apiVersion, '1.20.0')) {
      switch (activeFunction) {
        case 'function-c':
        case 'function-a':
        case 'function-f':
          return true;
      }
    }
    return false;
  };
  isWarningActive = function(activeFunction) {
    switch (activeFunction) {
      case 'function-l':
      case 'function-s':
      case 'function-g':
        return false;
      case 'function-r':
      case 'function-b':
        if (semver.lt(CONFIG.apiVersion, '1.20.0')) {
          return false;
        }
        break;
      default:
        return true;
        break;
    }
  };
  isVtxActive = function(activeFunction) {
    if (semver.gte(CONFIG.apiVersion, '1.36.0')) {
      switch (activeFunction) {
        case 'function-v':
        case 'function-c':
        case 'function-a':
        case 'function-f':
          return true;
        default:
          return false;
          break;
      }
    }
  };
  setOptionalGroupsVisibility = function() {
    var activeFunction;
    activeFunction = $('select.functionSelect').val();
    $('select.functionSelect').addClass(activeFunction);
    if (semver.lte(CONFIG.apiVersion, '1.18.0')) {
      $('.extra_functions20').hide();
      $('.mode_colors').hide();
    } else {
      $('.extra_functions20').show();
      $('.mode_colors').show();
    }
    $('.overlays').hide();
    $('.modifiers').hide();
    $('.blinkers').hide();
    $('.warningOverlay').hide();
    $('.vtxOverlay').hide();
    if (areOverlaysActive(activeFunction)) {
      $('.overlays').show();
    }
    if (areModifiersActive(activeFunction)) {
      $('.modifiers').show();
    }
    if (areBlinkersActive(activeFunction)) {
      $('.blinkers').show();
    }
    if (isWarningActive(activeFunction)) {
      $('.warningOverlay').show();
    }
    if (isVtxActive(activeFunction)) {
      $('.vtxOverlay').show();
    }
    if (semver.lt(CONFIG.apiVersion, '1.20.0')) {
      switch (activeFunction) {
        case 'function-r':
          $('.indicatorOverlay').hide();
          $('.directions').hide();
          break;
        default:
          $('.indicatorOverlay').show();
          $('.directions').show();
          break;
      }
    }
    $('.mode_colors').hide();
    if (semver.gte(CONFIG.apiVersion, '1.19.0')) {
      if (semver.gte(CONFIG.apiVersion, '1.20.0')) {
        if (activeFunction === 'function-f') {
          $('.mode_colors').show();
        }
      }
      $('.special_colors').show();
      $('.mode_color-6-0').hide();
      $('.mode_color-6-1').hide();
      $('.mode_color-6-2').hide();
      $('.mode_color-6-3').hide();
      $('.mode_color-6-4').hide();
      $('.mode_color-6-5').hide();
      $('.mode_color-6-6').hide();
      $('.mode_color-6-7').hide();
      switch (activeFunction) {
        case '':
        case 'function-f':
        case 'function-l':
          $('.special_colors').hide();
          break;
        case 'function-g':
          $('.mode_color-6-5').show();
          $('.mode_color-6-6').show();
          $('.mode_color-6-7').show();
          break;
        case 'function-b':
          $('.mode_color-6-4').show();
          break;
        case 'function-a':
          $('.mode_color-6-0').show();
          $('.mode_color-6-1').show();
          break;
        default:
          $('.special_colors').hide();
          break;
      }
    }
  };
  applyFunctionToSelectedLeds = function() {
    var activeFunction;
    activeFunction = $('select.functionSelect').val();
    TABS.led_strip.baseFuncs.forEach(function(letter) {
      if (activeFunction === 'function-' + letter) {
        $('select.functionSelect').addClass('function-' + letter);
        $('.ui-selected').find('.wire').each(function() {
          if ($(this).text() !== '') {
            $(this).parent().addClass('function-' + letter);
          }
        });
        unselectOverlays(letter);
      } else {
        $('select.functionSelect').removeClass('function-' + letter);
        $('.ui-selected').removeClass('function-' + letter);
      }
      if (activeFunction === '') {
        unselectOverlays(activeFunction);
      }
    });
  };
  unselectOverlays = function(letter) {
    if (semver.lt(CONFIG.apiVersion, '1.20.0')) {
      if (letter === 'b' || letter === 'r') {
        unselectOverlay(letter, 'i');
      }
      if (letter === 'b' || letter === 'r' || letter === 'l' || letter === 'g') {
        unselectOverlay(letter, 'w');
        unselectOverlay(letter, 'v');
        unselectOverlay(letter, 't');
        unselectOverlay(letter, 's');
      }
    } else {
      if (letter === 'r' || letter === '') {
        unselectOverlay(letter, 'o');
        unselectOverlay(letter, 'b');
        unselectOverlay(letter, 'n');
        unselectOverlay(letter, 't');
      }
      if (letter === 'l' || letter === 'g' || letter === 's') {
        unselectOverlay(letter, 'w');
        unselectOverlay(letter, 'v');
        unselectOverlay(letter, 't');
        unselectOverlay(letter, 'o');
        unselectOverlay(letter, 'b');
        unselectOverlay(letter, 'n');
      }
    }
  };
  unselectOverlay = function(func, overlay) {
    $('input.function-' + overlay).prop('checked', false);
    $('input.function-' + overlay).change();
    $('.ui-selected').each(function() {
      if (func === '' || $(this).is('.function-' + func)) {
        $(this).removeClass('function-' + overlay);
      }
    });
  };
  updateColors = function(value, hsvIndex) {
    var change, className;
    change = false;
    value = Number(value);
    className = '.color-' + selectedColorIndex;
    if ($(className).hasClass('btnOn')) {
      switch (hsvIndex) {
        case 0:
          if (LED_COLORS[selectedColorIndex].h !== value) {
            LED_COLORS[selectedColorIndex].h = value;
            $('.colorDefineSliderValue.Hvalue').text(LED_COLORS[selectedColorIndex].h);
            change = true;
          }
          break;
        case 1:
          if (LED_COLORS[selectedColorIndex].s !== value) {
            LED_COLORS[selectedColorIndex].s = value;
            $('.colorDefineSliderValue.Svalue').text(LED_COLORS[selectedColorIndex].s);
            change = true;
          }
          break;
        case 2:
          if (LED_COLORS[selectedColorIndex].v !== value) {
            LED_COLORS[selectedColorIndex].v = value;
            $('.colorDefineSliderValue.Vvalue').text(LED_COLORS[selectedColorIndex].v);
            change = true;
          }
      }
    }
    $('.colors').children().each(function() {
      setBackgroundColor($(this));
    });
    $('.overlay-color').each(function() {
      setBackgroundColor($(this));
    });
    $('.mode_colors').each(function() {
      setModeBackgroundColor($(this));
    });
    $('.special_colors').each(function() {
      setModeBackgroundColor($(this));
    });
    if (change) {
      updateBulkCmd();
    }
  };
  drawColorBoxesInColorLedPoints = function() {
    $('.gPoint').each(function() {
      var className, colorIndex;
      if ($(this).is('.function-c') || $(this).is('.function-r') || $(this).is('.function-b')) {
        $(this).find('.overlay-color').show();
        colorIndex = 0;
        while (colorIndex < 16) {
          className = 'color-' + colorIndex;
          if ($(this).is('.' + className)) {
            $(this).find('.overlay-color').addClass(className);
            $(this).find('.overlay-color').css('background-color', HsvToColor(LED_COLORS[colorIndex]));
          } else {
            if ($(this).find('.overlay-color').is('.' + className)) {
              $(this).find('.overlay-color').removeClass(className);
            }
          }
          colorIndex++;
        }
      } else {
        $(this).find('.overlay-color').hide();
      }
    });
  };
  setColorSliders = function(colorIndex) {
    var change, sliders;
    sliders = $('div.colorDefineSliders input');
    change = false;
    if (!LED_COLORS[colorIndex]) {
      return;
    }
    if (LED_COLORS[colorIndex].h !== Number(sliders.eq(0).val())) {
      sliders.eq(0).val(LED_COLORS[colorIndex].h);
      $('.colorDefineSliderValue.Hvalue').text(LED_COLORS[colorIndex].h);
      change = true;
    }
    if (LED_COLORS[colorIndex].s !== Number(sliders.eq(1).val())) {
      sliders.eq(1).val(LED_COLORS[colorIndex].s);
      $('.colorDefineSliderValue.Svalue').text(LED_COLORS[colorIndex].s);
      change = true;
    }
    if (LED_COLORS[colorIndex].v !== Number(sliders.eq(2).val())) {
      sliders.eq(2).val(LED_COLORS[colorIndex].v);
      $('.colorDefineSliderValue.Vvalue').text(LED_COLORS[colorIndex].v);
      change = true;
    }
    if (change) {
      sliders.trigger('input');
    }
  };
  HsvToColor = function(input) {
    var HSL, HSV, ret;
    if (input === void 0) {
      return '';
    }
    HSV = {
      h: Number(input.h),
      s: Number(input.s),
      v: Number(input.v)
    };
    if (HSV.s === 0 && HSV.v === 0) {
      return '';
    }
    HSV = {
      h: HSV.h,
      s: 1 - (HSV.s / 255),
      v: HSV.v / 255
    };
    HSL = {
      h: 0,
      s: 0,
      v: 0
    };
    HSL.h = HSV.h;
    HSL.l = (2 - HSV.s) * HSV.v / 2;
    HSL.s = HSL.l && HSL.l < 1 ? HSV.s * HSV.v / (HSL.l < 0.5 ? HSL.l * 2 : 2 - (HSL.l * 2)) : HSL.s;
    ret = 'hsl(' + HSL.h + ', ' + HSL.s * 100 + '%, ' + HSL.l * 100 + '%)';
    return ret;
  };
  getModeColor = function(mode, dir) {
    var i, mc;
    i = 0;
    while (i < LED_MODE_COLORS.length) {
      mc = LED_MODE_COLORS[i];
      if (mc.mode === mode && mc.direction === dir) {
        return mc.color;
      }
      i++;
    }
    return '';
  };
  setModeColor = function(mode, dir, color) {
    var i, mc;
    i = 0;
    while (i < LED_MODE_COLORS.length) {
      mc = LED_MODE_COLORS[i];
      if (mc.mode === mode && mc.direction === dir) {
        mc.color = color;
        return 1;
      }
      i++;
    }
    return 0;
  };
  clearModeColorSelection = function() {
    selectedModeColor = null;
    $('.mode_colors').each(function() {
      $(this).children().each(function() {
        if ($(this).is('.btnOn')) {
          $(this).removeClass('btnOn');
        }
      });
    });
  };
  if (semver.lt(CONFIG.apiVersion, '1.20.0')) {
    TABS.led_strip.functions = ['i', 'w', 'f', 'a', 't', 'r', 'c', 'g', 's', 'b'];
    TABS.led_strip.baseFuncs = ['c', 'f', 'a', 'b', 'g', 'r'];
    TABS.led_strip.overlays = ['t', 's', 'i', 'w'];
  } else {
    TABS.led_strip.functions = ['i', 'w', 'f', 'a', 't', 'r', 'c', 'g', 's', 'b', 'l', 'o', 'n'];
    TABS.led_strip.baseFuncs = ['c', 'f', 'a', 'l', 's', 'g', 'r'];
    if (semver.lt(CONFIG.apiVersion, '1.36.0')) {
      TABS.led_strip.overlays = ['t', 'o', 'b', 'n', 'i', 'w'];
    } else {
      TABS.led_strip.overlays = ['t', 'o', 'b', 'v', 'i', 'w'];
    }
  }
  TABS.led_strip.wireMode = false;
  if (GUI.active_tab !== 'led_strip') {
    GUI.active_tab = 'led_strip';
  }
  load_led_config();
};

TABS.led_strip.cleanup = function(callback) {
  if (callback) {
    callback();
  }
};
