// Generated by CoffeeScript 1.12.7
var FONT, OSD, STICK_OVERLAY_SPRITE, SYM, _arrayLikeToArray, _createForOfIteratorHelper, _typeof, _unsupportedIterableToArray, drawCanvas;

_typeof = function(o) {
  '@babel/helpers - typeof';
  _typeof = 'function' === typeof Symbol && 'symbol' === typeof Symbol.iterator ? (function(o) {
    return typeof o;
  }) : (function(o) {
    if (o && 'function' === typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype) {
      return 'symbol';
    } else {
      return typeof o;
    }
  });
  return _typeof(o);
};

_createForOfIteratorHelper = function(r, e) {
  var F, _n, a, o, t, u;
  t = 'undefined' !== typeof Symbol && r[Symbol.iterator] || r['@@iterator'];
  if (!t) {
    if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && 'number' === typeof r.length) {
      t && (r = t);
      _n = 0;
      F = function() {};
      return {
        s: F,
        n: function() {
          if (_n >= r.length) {
            return {
              done: !0
            };
          } else {
            return {
              done: !1,
              value: r[_n++]
            };
          }
        },
        e: function(r) {
          throw r;
        },
        f: F
      };
    }
    throw new TypeError('Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.');
  }
  o = void 0;
  a = !0;
  u = !1;
  return {
    s: function() {
      t = t.call(r);
    },
    n: function() {
      var r;
      r = t.next();
      a = r.done;
      return r;
    },
    e: function(r) {
      u = !0;
      o = r;
    },
    f: function() {
      if (a || null === t['return'] || t['return']()) {
        true;
      }
      if (u) {
        throw o;
      }
    }
  };
};

_unsupportedIterableToArray = function(r, a) {
  var t;
  if (r) {
    if ('string' === typeof r) {
      return _arrayLikeToArray(r, a);
    }
    t = {}.toString.call(r).slice(8, -1);
    return 'Object' === t && r.constructor && (t = r.constructor.name);
    if ('Map' === t || 'Set' === t) {
      Array.from(r);
    } else if ('Arguments' === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t)) {
      _arrayLikeToArray(r, a);
    } else {
      void 0;
    }
  }
};

_arrayLikeToArray = function(r, a) {
  var e, n;
  (null === a || a > r.length) && (a = r.length);
  e = 0;
  n = Array(a);
  while (e < a) {
    n[e] = r[e];
    e++;
  }
  return n;
};

'use strict';

SYM = SYM || {};

SYM.loadSymbols = function() {
  SYM.BLANK = 0x20;
  SYM.VOLT = 0x06;
  SYM.RSSI = 0x01;
  SYM.LINK_QUALITY = 0x7B;
  SYM.AH_RIGHT = 0x02;
  SYM.AH_LEFT = 0x03;
  SYM.THR = 0x04;
  SYM.FLY_M = 0x9C;
  SYM.ON_M = 0x9B;
  SYM.AH_CENTER_LINE = 0x72;
  SYM.AH_CENTER = 0x73;
  SYM.AH_CENTER_LINE_RIGHT = 0x74;
  SYM.AH_BAR9_0 = 0x80;
  SYM.AH_DECORATION = 0x13;
  SYM.LOGO = 0xA0;
  SYM.AMP = 0x9A;
  SYM.MAH = 0x07;
  SYM.METRE = 0xC;
  SYM.FEET = 0xF;
  SYM.KPH = 0x9E;
  SYM.MPH = 0x9D;
  SYM.MPS = 0x9F;
  SYM.FTPS = 0x99;
  SYM.SPEED = 0x70;
  SYM.TOTAL_DIST = 0x71;
  SYM.GPS_SAT_L = 0x1E;
  SYM.GPS_SAT_R = 0x1F;
  SYM.GPS_LAT = 0x89;
  SYM.GPS_LON = 0x98;
  SYM.HOMEFLAG = 0x11;
  SYM.PB_START = 0x8A;
  SYM.PB_FULL = 0x8B;
  SYM.PB_EMPTY = 0x8D;
  SYM.PB_END = 0x8E;
  SYM.PB_CLOSE = 0x8F;
  SYM.BATTERY = 0x96;
  SYM.ARROW_NORTH = 0x68;
  SYM.ARROW_SOUTH = 0x60;
  SYM.ARROW_EAST = 0x64;
  SYM.ARROW_SMALL_UP = 0x75;
  SYM.HEADING_LINE = 0x1D;
  SYM.HEADING_DIVIDED_LINE = 0x1C;
  SYM.HEADING_N = 0x18;
  SYM.HEADING_S = 0x19;
  SYM.HEADING_E = 0x1A;
  SYM.HEADING_W = 0x1B;
  SYM.TEMPERATURE = 0x7A;
  SYM.TEMP_F = 0x0D;
  SYM.TEMP_C = 0x0E;
  SYM.STICK_OVERLAY_SPRITE_HIGH = 0x08;
  SYM.STICK_OVERLAY_SPRITE_MID = 0x09;
  SYM.STICK_OVERLAY_SPRITE_LOW = 0x0A;
  SYM.STICK_OVERLAY_CENTER = 0x0B;
  SYM.STICK_OVERLAY_VERTICAL = 0x16;
  SYM.STICK_OVERLAY_HORIZONTAL = 0x17;
  SYM.BBLOG = 0x10;
  SYM.ALTITUDE = 0x7F;
  SYM.PITCH = 0x15;
  SYM.ROLL = 0x14;

  /* Versions before firmware 4.1 use font V1
   * To maintain this list at minimum, we only add here: 
   * - Symbols used in this versions
   * - That were moved or didn't exist in the font file
   */
  if (semver.lt(CONFIG.apiVersion, '1.42.0')) {
    SYM.AH_CENTER_LINE = 0x26;
    SYM.AH_CENTER = 0x7E;
    SYM.AH_CENTER_LINE_RIGHT = 0x27;
    SYM.SPEED = null;
    SYM.LINK_QUALITY = null;
  }
};

STICK_OVERLAY_SPRITE = [SYM.STICK_OVERLAY_SPRITE_HIGH, SYM.STICK_OVERLAY_SPRITE_MID, SYM.STICK_OVERLAY_SPRITE_LOW];

FONT = FONT || {};

FONT.initData = function() {
  if (FONT.data) {
    return;
  }
  FONT.data = {
    loaded_font_file: 'default',
    characters_bytes: [],
    characters: [],
    character_image_urls: []
  };
};

FONT.constants = {
  MAX_CHAR_COUNT: 256,
  SIZES: {
    MAX_NVM_FONT_CHAR_SIZE: 54,
    MAX_NVM_FONT_CHAR_FIELD_SIZE: 64,
    CHAR_HEIGHT: 18,
    CHAR_WIDTH: 12,
    LINE: 30
  },
  COLORS: {
    0: 'rgba(0, 0, 0, 1)',
    1: 'rgba(255, 255, 255, 0)',
    2: 'rgba(255,255,255, 1)'
  }
};


/**
* Each line is composed of 8 asci 1 or 0, representing 1 bit each for a total of 1 byte per line
 */

FONT.parseMCMFontFile = function(data) {
  var data;
  var character_bits, character_bytes, i, line, msg, pushChar, v, y;
  data = data.trim().split('\n');
  FONT.data.characters.length = 0;
  FONT.data.characters_bytes.length = 0;
  FONT.data.character_image_urls.length = 0;
  LogoManager.resetImageInfo();
  if (data.shift().trim() !== 'MAX7456') {
    msg = 'that font file doesnt have the MAX7456 header, giving up';
    console.debug(msg);
    Promise.reject(msg);
  }
  character_bits = [];
  character_bytes = [];
  FONT.data.hexstring = [];
  pushChar = function() {
    if (character_bytes.length !== FONT.constants.SIZES.MAX_NVM_FONT_CHAR_FIELD_SIZE) {
      return;
    }
    FONT.data.characters_bytes.push(character_bytes);
    FONT.data.characters.push(character_bits);
    FONT.draw(FONT.data.characters.length - 1);
    character_bits = [];
    character_bytes = [];
  };
  i = 0;
  while (i < data.length) {
    line = data[i];
    FONT.data.hexstring.push('0x' + parseInt(line, 2).toString(16));
    if (character_bits.length === FONT.constants.SIZES.MAX_NVM_FONT_CHAR_FIELD_SIZE * 8 / 2) {
      pushChar();
    }
    y = 0;
    while (y < 8) {
      v = parseInt(line.slice(y, y + 2), 2);
      character_bits.push(v);
      y = y + 2;
    }
    character_bytes.push(parseInt(line, 2));
    i++;
  }
  pushChar();
  return FONT.data.characters;
};

FONT.openFontFile = function(fontPreviewElement) {
  return new Promise(function(resolve) {
    chrome.fileSystem.chooseEntry({
      type: 'openFile',
      accepts: [
        {
          description: 'MCM files',
          extensions: ['mcm']
        }
      ]
    }, function(fileEntry) {
      FONT.data.loaded_font_file = fileEntry.name;
      if (chrome.runtime.lastError) {
        console.error(chrome.runtime.lastError.message);
        return;
      }
      fileEntry.file(function(file) {
        var reader;
        reader = new FileReader;
        reader.onloadend = function(e) {
          if (e.total !== 0 && e.total === e.loaded) {
            FONT.parseMCMFontFile(e.target.result);
            resolve();
          } else {
            console.error('could not load whole font file');
          }
        };
        reader.readAsText(file);
      });
    });
  });
};


/**
* returns a canvas image with the character on it
 */

drawCanvas = function(charAddress) {
  var canvas, ctx, height, pixelSize, v, width, x, y;
  canvas = document.createElement('canvas');
  ctx = canvas.getContext('2d');
  pixelSize = pixelSize || 1;
  width = pixelSize * FONT.constants.SIZES.CHAR_WIDTH;
  height = pixelSize * FONT.constants.SIZES.CHAR_HEIGHT;
  canvas.width = width;
  canvas.height = height;
  y = 0;
  while (y < height) {
    x = 0;
    while (x < width) {
      if (!(charAddress in FONT.data.characters)) {
        console.log('charAddress', charAddress, ' is not in ', FONT.data.characters.length);
      }
      v = FONT.data.characters[charAddress][y * width + x];
      ctx.fillStyle = FONT.constants.COLORS[v];
      ctx.fillRect(x, y, pixelSize, pixelSize);
      x++;
    }
    y++;
  }
  return canvas;
};

FONT.draw = function(charAddress) {
  var cached;
  cached = FONT.data.character_image_urls[charAddress];
  if (!cached) {
    cached = FONT.data.character_image_urls[charAddress] = drawCanvas(charAddress).toDataURL('image/png');
  }
  return cached;
};

FONT.msp = {
  encode: function(charAddress) {
    return [charAddress].concat(FONT.data.characters_bytes[charAddress].slice(0, FONT.constants.SIZES.MAX_NVM_FONT_CHAR_SIZE));
  }
};

FONT.upload = function($progress) {
  return Promise.mapSeries(FONT.data.characters, function(data, i) {
    $progress.val(i / FONT.data.characters.length * 100);
    return MSP.promise(MSPCodes.MSP_OSD_CHAR_WRITE, FONT.msp.encode(i));
  }).then(function() {
    console.log('Uploaded all ' + FONT.data.characters.length + ' characters');
    GUI.log(i18n.getMessage('osdSetupUploadingFontEnd', {
      length: FONT.data.characters.length
    }));
    OSD.GUI.fontManager.close();
    return MSP.promise(MSPCodes.MSP_SET_REBOOT);
  });
};

FONT.preview = function($el) {
  var i, url;
  $el.empty();
  i = 0;
  while (i < SYM.LOGO) {
    url = FONT.data.character_image_urls[i];
    $el.append('<img src="' + url + '" title="0x' + i.toString(16) + '"></img>');
    i++;
  }
};

FONT.symbol = function(hexVal) {
  if (hexVal === '' || hexVal === null) {
    return '';
  } else {
    return String.fromCharCode(hexVal);
  }
};

OSD = OSD || {};

OSD.getNumberOfProfiles = function() {
  return OSD.data.osd_profiles.number;
};

OSD.getCurrentPreviewProfile = function() {
  var osdprofile_e;
  osdprofile_e = $('.osdprofile-selector');
  if (osdprofile_e) {
    return osdprofile_e.val();
  } else {
    return 0;
  }
};

OSD.initData = function() {
  OSD.data = {
    video_system: null,
    unit_mode: null,
    alarms: [],
    stat_items: [],
    warnings: [],
    display_items: [],
    timers: [],
    last_positions: {},
    preview: [],
    tooltips: [],
    osd_profiles: {}
  };
};

OSD.initData();

OSD.generateTimerPreview = function(osd_data, timer_index) {
  var preview;
  preview = '';
  switch (osd_data.timers[timer_index].src) {
    case 0:
    case 3:
      preview += FONT.symbol(SYM.ON_M);
      break;
    case 1:
    case 2:
      preview += FONT.symbol(SYM.FLY_M);
  }
  switch (osd_data.timers[timer_index].precision) {
    case 0:
      preview += '00:00';
      break;
    case 1:
      preview += '00:00.00';
      break;
    case 2:
      preview += '00:00.0';
  }
  return preview;
};

OSD.generateTemperaturePreview = function(osd_data, temperature) {
  var preview;
  preview = FONT.symbol(SYM.TEMPERATURE);
  switch (osd_data.unit_mode) {
    case 0:
      temperature *= 9.0 / 5.0;
      temperature += 32.0;
      preview += Math.floor(temperature) + FONT.symbol(SYM.TEMP_F);
      break;
    case 1:
      preview += temperature + FONT.symbol(SYM.TEMP_C);
  }
  return preview;
};

OSD.generateCraftName = function(osd_data) {
  var preview;
  preview = 'CRAFT_NAME';
  if (CONFIG.name !== '') {
    preview = CONFIG.name.toUpperCase();
  }
  return preview;
};

OSD.generateDisplayName = function(osd_data) {
  var preview;
  preview = 'DISPLAY_NAME';
  if (CONFIG.displayName !== '') {
    preview = CONFIG.displayName.toUpperCase();
  }
  return preview;
};

OSD.drawStickOverlayPreview = function() {
  var OVERLAY_HEIGHT, OVERLAY_WIDTH, element, randomInt, stickOverlay, stickSymbol, stickX, stickY, symbol, x, y;
  OVERLAY_WIDTH = 7;
  OVERLAY_HEIGHT = 5;
  stickX = randomInt(OVERLAY_WIDTH);
  stickY = randomInt(OVERLAY_HEIGHT);
  stickSymbol = randomInt(3);
  stickOverlay = new Array;
  randomInt = function(count) {
    return Math.floor(Math.random() * Math.floor(count));
  };
  x = 0;
  while (x < OVERLAY_WIDTH) {
    y = 0;
    while (y < OVERLAY_HEIGHT) {
      symbol = void 0;
      if (x === stickX && y === stickY) {
        symbol = STICK_OVERLAY_SPRITE[stickSymbol];
      } else if (x === (OVERLAY_WIDTH - 1) / 2 && y === (OVERLAY_HEIGHT - 1) / 2) {
        symbol = SYM.STICK_OVERLAY_CENTER;
      } else if (x === (OVERLAY_WIDTH - 1) / 2) {
        symbol = SYM.STICK_OVERLAY_VERTICAL;
      } else if (y === (OVERLAY_HEIGHT - 1) / 2) {
        symbol = SYM.STICK_OVERLAY_HORIZONTAL;
      }
      if (symbol) {
        element = {
          x: x,
          y: y,
          sym: symbol
        };
        stickOverlay.push(element);
      }
      y++;
    }
    x++;
  }
  return stickOverlay;
};

OSD.loadDisplayFields = function() {
  OSD.ALL_DISPLAY_FIELDS = {
    MAIN_BATT_VOLTAGE: {
      name: 'MAIN_BATT_VOLTAGE',
      text: 'osdTextElementMainBattVoltage',
      desc: 'osdDescElementMainBattVoltage',
      default_position: -29,
      draw_order: 20,
      positionable: true,
      preview: FONT.symbol(SYM.BATTERY) + '16.8' + FONT.symbol(SYM.VOLT)
    },
    RSSI_VALUE: {
      name: 'RSSI_VALUE',
      text: 'osdTextElementRssiValue',
      desc: 'osdDescElementRssiValue',
      default_position: -59,
      draw_order: 30,
      positionable: true,
      preview: FONT.symbol(SYM.RSSI) + '99'
    },
    TIMER: {
      name: 'TIMER',
      text: 'osdTextElementTimer',
      desc: 'osdDescElementTimer',
      default_position: -39,
      positionable: true,
      preview: FONT.symbol(SYM.ON_M) + ' 11:11'
    },
    THROTTLE_POSITION: {
      name: 'THROTTLE_POSITION',
      text: 'osdTextElementThrottlePosition',
      desc: 'osdDescElementThrottlePosition',
      default_position: -9,
      draw_order: 110,
      positionable: true,
      preview: FONT.symbol(SYM.THR) + ' 69'
    },
    CPU_LOAD: {
      name: 'CPU_LOAD',
      text: 'osdTextElementCpuLoad',
      desc: 'osdDescElementCpuLoad',
      default_position: 26,
      positionable: true,
      preview: '15'
    },
    VTX_CHANNEL: {
      name: 'VTX_CHANNEL',
      text: 'osdTextElementVtxChannel',
      desc: 'osdDescElementVtxChannel',
      default_position: 1,
      draw_order: 120,
      positionable: true,
      preview: 'R:2:200:P'
    },
    VOLTAGE_WARNING: {
      name: 'VOLTAGE_WARNING',
      text: 'osdTextElementVoltageWarning',
      desc: 'osdDescElementVoltageWarning',
      default_position: -80,
      positionable: true,
      preview: 'LOW VOLTAGE'
    },
    ARMED: {
      name: 'ARMED',
      text: 'osdTextElementArmed',
      desc: 'osdDescElementArmed',
      default_position: -107,
      positionable: true,
      preview: 'ARMED'
    },
    DISARMED: {
      name: 'DISARMED',
      text: 'osdTextElementDisarmed',
      desc: 'osdDescElementDisarmed',
      default_position: -109,
      draw_order: 280,
      positionable: true,
      preview: 'DISARMED'
    },
    CROSSHAIRS: {
      name: 'CROSSHAIRS',
      text: 'osdTextElementCrosshairs',
      desc: 'osdDescElementCrosshairs',
      default_position: function() {
        var position;
        position = 193;
        if (OSD.constants.VIDEO_TYPES[OSD.data.video_system] !== 'NTSC') {
          position += FONT.constants.SIZES.LINE;
        }
        return position;
      },
      draw_order: 40,
      positionable: function() {
        if (semver.gte(CONFIG.apiVersion, '1.39.0')) {
          return true;
        } else {
          return false;
        }
      },
      preview: function() {
        return FONT.symbol(SYM.AH_CENTER_LINE) + FONT.symbol(SYM.AH_CENTER) + FONT.symbol(SYM.AH_CENTER_LINE_RIGHT);
      }
    },
    ARTIFICIAL_HORIZON: {
      name: 'ARTIFICIAL_HORIZON',
      text: 'osdTextElementArtificialHorizon',
      desc: 'osdDescElementArtificialHorizon',
      default_position: function() {
        var position;
        position = 74;
        if (OSD.constants.VIDEO_TYPES[OSD.data.video_system] !== 'NTSC') {
          position += FONT.constants.SIZES.LINE;
        }
        return position;
      },
      draw_order: 10,
      positionable: function() {
        if (semver.gte(CONFIG.apiVersion, '1.39.0')) {
          return true;
        } else {
          return false;
        }
      },
      preview: function() {
        var artificialHorizon, element, i, j;
        artificialHorizon = new Array;
        j = 1;
        while (j < 8) {
          i = -4;
          while (i <= 4) {
            element = void 0;
            if (j !== 4) {
              element = {
                x: i,
                y: j,
                sym: SYM.BLANK
              };
            } else {
              element = {
                x: i,
                y: j,
                sym: SYM.AH_BAR9_0 + 4
              };
            }
            artificialHorizon.push(element);
            i++;
          }
          j++;
        }
        return artificialHorizon;
      }
    },
    HORIZON_SIDEBARS: {
      name: 'HORIZON_SIDEBARS',
      text: 'osdTextElementHorizonSidebars',
      desc: 'osdDescElementHorizonSidebars',
      default_position: function() {
        var position;
        position = 194;
        if (OSD.constants.VIDEO_TYPES[OSD.data.video_system] !== 'NTSC') {
          position += FONT.constants.SIZES.LINE;
        }
        return position;
      },
      draw_order: 50,
      positionable: function() {
        if (semver.gte(CONFIG.apiVersion, '1.39.0')) {
          return true;
        } else {
          return false;
        }
      },
      preview: function(fieldPosition) {
        var element;
        var element, horizonSidebar, hudheight, hudwidth, i;
        horizonSidebar = new Array;
        hudwidth = OSD.constants.AHISIDEBARWIDTHPOSITION;
        hudheight = OSD.constants.AHISIDEBARHEIGHTPOSITION;
        i = -hudheight;
        while (i <= hudheight) {
          element = {
            x: -hudwidth,
            y: i,
            sym: SYM.AH_DECORATION
          };
          horizonSidebar.push(element);
          element = {
            x: hudwidth,
            y: i,
            sym: SYM.AH_DECORATION
          };
          horizonSidebar.push(element);
          i++;
        }
        element = {
          x: -hudwidth + 1,
          y: 0,
          sym: SYM.AH_LEFT
        };
        horizonSidebar.push(element);
        element = {
          x: hudwidth - 1,
          y: 0,
          sym: SYM.AH_RIGHT
        };
        horizonSidebar.push(element);
        return horizonSidebar;
      }
    },
    CURRENT_DRAW: {
      name: 'CURRENT_DRAW',
      text: 'osdTextElementCurrentDraw',
      desc: 'osdDescElementCurrentDraw',
      default_position: -23,
      draw_order: 130,
      positionable: true,
      preview: function() {
        if (semver.gte(CONFIG.apiVersion, '1.36.0')) {
          return ' 42.00' + FONT.symbol(SYM.AMP);
        } else {
          return FONT.symbol(SYM.AMP) + '42.0';
        }
      }
    },
    MAH_DRAWN: {
      name: 'MAH_DRAWN',
      text: 'osdTextElementMahDrawn',
      desc: 'osdDescElementMahDrawn',
      default_position: -18,
      draw_order: 140,
      positionable: true,
      preview: function() {
        if (semver.gte(CONFIG.apiVersion, '1.36.0')) {
          return ' 690' + FONT.symbol(SYM.MAH);
        } else {
          return FONT.symbol(SYM.MAH) + '690';
        }
      }
    },
    CRAFT_NAME: {
      name: 'CRAFT_NAME',
      text: 'osdTextElementCraftName',
      desc: 'osdDescElementCraftName',
      default_position: -77,
      draw_order: 150,
      positionable: true,
      preview: OSD.generateCraftName
    },
    ALTITUDE: {
      name: 'ALTITUDE',
      text: 'osdTextElementAltitude',
      desc: 'osdDescElementAltitude',
      default_position: 62,
      draw_order: 160,
      positionable: true,
      preview: function(osd_data) {
        return FONT.symbol(SYM.ALTITUDE) + '399.7' + FONT.symbol(osd_data.unit_mode === 0 ? SYM.FEET : SYM.METRE);
      }
    },
    ONTIME: {
      name: 'ONTIME',
      text: 'osdTextElementOnTime',
      desc: 'osdDescElementOnTime',
      default_position: -1,
      positionable: true,
      preview: FONT.symbol(SYM.ON_M) + '05:42'
    },
    FLYTIME: {
      name: 'FLYTIME',
      text: 'osdTextElementFlyTime',
      desc: 'osdDescElementFlyTime',
      default_position: -1,
      positionable: true,
      preview: FONT.symbol(SYM.FLY_M) + '04:11'
    },
    FLYMODE: {
      name: 'FLYMODE',
      text: 'osdTextElementFlyMode',
      desc: 'osdDescElementFlyMode',
      default_position: -1,
      draw_order: 90,
      positionable: true,
      preview: 'STAB'
    },
    GPS_SPEED: {
      name: 'GPS_SPEED',
      text: 'osdTextElementGPSSpeed',
      desc: 'osdDescElementGPSSpeed',
      default_position: -1,
      draw_order: 810,
      positionable: true,
      preview: function(osd_data) {
        return FONT.symbol(SYM.SPEED) + ' 40' + (osd_data.unit_mode === 0 ? FONT.symbol(SYM.MPH) : FONT.symbol(SYM.KPH));
      }
    },
    GPS_SATS: {
      name: 'GPS_SATS',
      text: 'osdTextElementGPSSats',
      desc: 'osdDescElementGPSSats',
      default_position: -1,
      draw_order: 800,
      positionable: true,
      preview: FONT.symbol(SYM.GPS_SAT_L) + FONT.symbol(SYM.GPS_SAT_R) + '14'
    },
    GPS_LON: {
      name: 'GPS_LON',
      text: 'osdTextElementGPSLon',
      desc: 'osdDescElementGPSLon',
      default_position: -1,
      draw_order: 830,
      positionable: true,
      preview: FONT.symbol(SYM.GPS_LON) + '-000.0000000'
    },
    GPS_LAT: {
      name: 'GPS_LAT',
      text: 'osdTextElementGPSLat',
      desc: 'osdDescElementGPSLat',
      default_position: -1,
      draw_order: 820,
      positionable: true,
      preview: FONT.symbol(SYM.GPS_LAT) + '-00.0000000 '
    },
    DEBUG: {
      name: 'DEBUG',
      text: 'osdTextElementDebug',
      desc: 'osdDescElementDebug',
      default_position: -1,
      draw_order: 240,
      positionable: true,
      preview: 'DBG     0     0     0     0'
    },
    PID_ROLL: {
      name: 'PID_ROLL',
      text: 'osdTextElementPIDRoll',
      desc: 'osdDescElementPIDRoll',
      default_position: 0x800 | 10 << 5 | 2,
      draw_order: 170,
      positionable: true,
      preview: 'ROL  43  40  20'
    },
    PID_PITCH: {
      name: 'PID_PITCH',
      text: 'osdTextElementPIDPitch',
      desc: 'osdDescElementPIDPitch',
      default_position: 0x800 | 11 << 5 | 2,
      draw_order: 180,
      positionable: true,
      preview: 'PIT  58  50  22'
    },
    PID_YAW: {
      name: 'PID_YAW',
      text: 'osdTextElementPIDYaw',
      desc: 'osdDescElementPIDYaw',
      default_position: 0x800 | 12 << 5 | 2,
      draw_order: 190,
      positionable: true,
      preview: 'YAW  70  45  20'
    },
    POWER: {
      name: 'POWER',
      text: 'osdTextElementPower',
      desc: 'osdDescElementPower',
      default_position: 15 << 5 | 2,
      draw_order: 200,
      positionable: true,
      preview: function() {
        if (semver.gte(CONFIG.apiVersion, '1.36.0')) {
          return ' 142W';
        } else {
          return '142W';
        }
      }
    },
    PID_RATE_PROFILE: {
      name: 'PID_RATE_PROFILE',
      text: 'osdTextElementPIDRateProfile',
      desc: 'osdDescElementPIDRateProfile',
      default_position: 0x800 | 13 << 5 | 2,
      draw_order: 210,
      positionable: true,
      preview: '1-2'
    },
    BATTERY_WARNING: {
      name: 'BATTERY_WARNING',
      text: 'osdTextElementBatteryWarning',
      desc: 'osdDescElementBatteryWarning',
      default_position: -1,
      positionable: true,
      preview: 'LOW VOLTAGE'
    },
    AVG_CELL_VOLTAGE: {
      name: 'AVG_CELL_VOLTAGE',
      text: 'osdTextElementAvgCellVoltage',
      desc: 'osdDescElementAvgCellVoltage',
      default_position: 12 << 5,
      draw_order: 230,
      positionable: true,
      preview: FONT.symbol(SYM.BATTERY) + '3.98' + FONT.symbol(SYM.VOLT)
    },
    PITCH_ANGLE: {
      name: 'PITCH_ANGLE',
      text: 'osdTextElementPitchAngle',
      desc: 'osdDescElementPitchAngle',
      default_position: -1,
      draw_order: 250,
      positionable: true,
      preview: FONT.symbol(SYM.PITCH) + '-00.0'
    },
    ROLL_ANGLE: {
      name: 'ROLL_ANGLE',
      text: 'osdTextElementRollAngle',
      desc: 'osdDescElementRollAngle',
      default_position: -1,
      draw_order: 260,
      positionable: true,
      preview: FONT.symbol(SYM.ROLL) + '-00.0'
    },
    MAIN_BATT_USAGE: {
      name: 'MAIN_BATT_USAGE',
      text: 'osdTextElementMainBattUsage',
      desc: 'osdDescElementMainBattUsage',
      default_position: -17,
      draw_order: 270,
      positionable: true,
      preview: FONT.symbol(SYM.PB_START) + FONT.symbol(SYM.PB_FULL) + FONT.symbol(SYM.PB_FULL) + FONT.symbol(SYM.PB_FULL) + FONT.symbol(SYM.PB_FULL) + FONT.symbol(SYM.PB_FULL) + FONT.symbol(SYM.PB_FULL) + FONT.symbol(SYM.PB_FULL) + FONT.symbol(SYM.PB_FULL) + FONT.symbol(SYM.PB_FULL) + FONT.symbol(SYM.PB_END) + FONT.symbol(SYM.PB_EMPTY) + FONT.symbol(SYM.PB_CLOSE)
    },
    ARMED_TIME: {
      name: 'ARMED_TIME',
      text: 'osdTextElementArmedTime',
      desc: 'osdDescElementArmedTime',
      default_position: -1,
      positionable: true,
      preview: FONT.symbol(SYM.FLY_M) + '02:07'
    },
    HOME_DIR: {
      name: 'HOME_DIRECTION',
      text: 'osdTextElementHomeDirection',
      desc: 'osdDescElementHomeDirection',
      default_position: -1,
      draw_order: 850,
      positionable: true,
      preview: FONT.symbol(SYM.ARROW_SOUTH + 2)
    },
    HOME_DIST: {
      name: 'HOME_DISTANCE',
      text: 'osdTextElementHomeDistance',
      desc: 'osdDescElementHomeDistance',
      default_position: -1,
      draw_order: 840,
      positionable: true,
      preview: function(osd_data) {
        return FONT.symbol(SYM.HOMEFLAG) + '432' + FONT.symbol(osd_data.unit_mode === 0 ? SYM.FEET : SYM.METRE);
      }
    },
    NUMERICAL_HEADING: {
      name: 'NUMERICAL_HEADING',
      text: 'osdTextElementNumericalHeading',
      desc: 'osdDescElementNumericalHeading',
      default_position: -1,
      draw_order: 290,
      positionable: true,
      preview: FONT.symbol(SYM.ARROW_EAST) + '90'
    },
    NUMERICAL_VARIO: {
      name: 'NUMERICAL_VARIO',
      text: 'osdTextElementNumericalVario',
      desc: 'osdDescElementNumericalVario',
      default_position: -1,
      draw_order: 300,
      positionable: true,
      preview: function(osd_data) {
        return FONT.symbol(SYM.ARROW_SMALL_UP) + '8.7' + (osd_data.unit_mode === 0 ? FONT.symbol(SYM.FTPS) : FONT.symbol(SYM.MPS));
      }
    },
    COMPASS_BAR: {
      name: 'COMPASS_BAR',
      text: 'osdTextElementCompassBar',
      desc: 'osdDescElementCompassBar',
      default_position: -1,
      draw_order: 310,
      positionable: true,
      preview: function(osd_data) {
        return FONT.symbol(SYM.HEADING_W) + FONT.symbol(SYM.HEADING_LINE) + FONT.symbol(SYM.HEADING_DIVIDED_LINE) + FONT.symbol(SYM.HEADING_LINE) + FONT.symbol(SYM.HEADING_N) + FONT.symbol(SYM.HEADING_LINE) + FONT.symbol(SYM.HEADING_DIVIDED_LINE) + FONT.symbol(SYM.HEADING_LINE) + FONT.symbol(SYM.HEADING_E);
      }
    },
    WARNINGS: {
      name: 'WARNINGS',
      text: 'osdTextElementWarnings',
      desc: 'osdDescElementWarnings',
      default_position: -1,
      draw_order: 220,
      positionable: true,
      preview: 'LOW VOLTAGE'
    },
    ESC_TEMPERATURE: {
      name: 'ESC_TEMPERATURE',
      text: 'osdTextElementEscTemperature',
      desc: 'osdDescElementEscTemperature',
      default_position: -1,
      draw_order: 900,
      positionable: true,
      preview: function(osd_data) {
        return 'E' + OSD.generateTemperaturePreview(osd_data, 45);
      }
    },
    ESC_RPM: {
      name: 'ESC_RPM',
      text: 'osdTextElementEscRpm',
      desc: 'osdDescElementEscRpm',
      default_position: -1,
      draw_order: 1000,
      positionable: true,
      preview: ['22600', '22600', '22600', '22600']
    },
    REMAINING_TIME_ESTIMATE: {
      name: 'REMAINING_TIME_ESTIMATE',
      text: 'osdTextElementRemaningTimeEstimate',
      desc: 'osdDescElementRemaningTimeEstimate',
      default_position: -1,
      draw_order: 80,
      positionable: true,
      preview: '01:13'
    },
    RTC_DATE_TIME: {
      name: 'RTC_DATE_TIME',
      text: 'osdTextElementRtcDateTime',
      desc: 'osdDescElementRtcDateTime',
      default_position: -1,
      draw_order: 360,
      positionable: true,
      preview: '2017-11-11 16:20:00'
    },
    ADJUSTMENT_RANGE: {
      name: 'ADJUSTMENT_RANGE',
      text: 'osdTextElementAdjustmentRange',
      desc: 'osdDescElementAdjustmentRange',
      default_position: -1,
      draw_order: 370,
      positionable: true,
      preview: 'PITCH/ROLL P: 42'
    },
    TIMER_1: {
      name: 'TIMER_1',
      text: 'osdTextElementTimer1',
      desc: 'osdDescElementTimer1',
      default_position: -1,
      draw_order: 60,
      positionable: true,
      preview: function(osd_data) {
        return OSD.generateTimerPreview(osd_data, 0);
      }
    },
    TIMER_2: {
      name: 'TIMER_2',
      text: 'osdTextElementTimer2',
      desc: 'osdDescElementTimer2',
      default_position: -1,
      draw_order: 70,
      positionable: true,
      preview: function(osd_data) {
        return OSD.generateTimerPreview(osd_data, 1);
      }
    },
    CORE_TEMPERATURE: {
      name: 'CORE_TEMPERATURE',
      text: 'osdTextElementCoreTemperature',
      desc: 'osdDescElementCoreTemperature',
      default_position: -1,
      draw_order: 380,
      positionable: true,
      preview: function(osd_data) {
        return 'C' + OSD.generateTemperaturePreview(osd_data, 33);
      }
    },
    ANTI_GRAVITY: {
      name: 'ANTI_GRAVITY',
      text: 'osdTextAntiGravity',
      desc: 'osdDescAntiGravity',
      default_position: -1,
      draw_order: 320,
      positionable: true,
      preview: 'AG'
    },
    G_FORCE: {
      name: 'G_FORCE',
      text: 'osdTextGForce',
      desc: 'osdDescGForce',
      default_position: -1,
      draw_order: 15,
      positionable: true,
      preview: '1.0G'
    },
    MOTOR_DIAG: {
      name: 'MOTOR_DIAGNOSTICS',
      text: 'osdTextElementMotorDiag',
      desc: 'osdDescElementMotorDiag',
      default_position: -1,
      draw_order: 335,
      positionable: true,
      preview: FONT.symbol(0x84) + FONT.symbol(0x85) + FONT.symbol(0x84) + FONT.symbol(0x83)
    },
    LOG_STATUS: {
      name: 'LOG_STATUS',
      text: 'osdTextElementLogStatus',
      desc: 'osdDescElementLogStatus',
      default_position: -1,
      draw_order: 330,
      positionable: true,
      preview: FONT.symbol(SYM.BBLOG) + '16'
    },
    FLIP_ARROW: {
      name: 'FLIP_ARROW',
      text: 'osdTextElementFlipArrow',
      desc: 'osdDescElementFlipArrow',
      default_position: -1,
      draw_order: 340,
      positionable: true,
      preview: FONT.symbol(SYM.ARROW_EAST)
    },
    LINK_QUALITY: {
      name: 'LINK_QUALITY',
      text: 'osdTextElementLinkQuality',
      desc: 'osdDescElementLinkQuality',
      default_position: -1,
      draw_order: 390,
      positionable: true,
      preview: FONT.symbol(SYM.LINK_QUALITY) + '8'
    },
    FLIGHT_DIST: {
      name: 'FLIGHT_DISTANCE',
      text: 'osdTextElementFlightDist',
      desc: 'osdDescElementFlightDist',
      default_position: -1,
      draw_order: 860,
      positionable: true,
      preview: function(osd_data) {
        return FONT.symbol(SYM.TOTAL_DIST) + '653' + FONT.symbol(osd_data.unit_mode === 0 ? SYM.FEET : SYM.METRE);
      }
    },
    STICK_OVERLAY_LEFT: {
      name: 'STICK_OVERLAY_LEFT',
      text: 'osdTextElementStickOverlayLeft',
      desc: 'osdDescElementStickOverlayLeft',
      default_position: -1,
      draw_order: 400,
      positionable: true,
      preview: OSD.drawStickOverlayPreview
    },
    STICK_OVERLAY_RIGHT: {
      name: 'STICK_OVERLAY_RIGHT',
      text: 'osdTextElementStickOverlayRight',
      desc: 'osdDescElementStickOverlayRight',
      default_position: -1,
      draw_order: 410,
      positionable: true,
      preview: OSD.drawStickOverlayPreview
    },
    DISPLAY_NAME: {
      name: 'DISPLAY_NAME',
      text: 'osdTextElementDisplayName',
      desc: 'osdDescElementDisplayName',
      default_position: -77,
      draw_order: 350,
      positionable: true,
      preview: function(osd_data) {
        return OSD.generateDisplayName(osd_data, 1);
      }
    },
    ESC_RPM_FREQ: {
      name: 'ESC_RPM_FREQ',
      text: 'osdTextElementEscRpmFreq',
      desc: 'osdDescElementEscRpmFreq',
      default_position: -1,
      draw_order: 1010,
      positionable: true,
      preview: ['22600', '22600', '22600', '22600']
    },
    RATE_PROFILE_NAME: {
      name: 'RATE_PROFILE_NAME',
      text: 'osdTextElementRateProfileName',
      desc: 'osdDescElementRateProfileName',
      default_position: -1,
      draw_order: 420,
      positionable: true,
      preview: 'RATE_1'
    },
    PID_PROFILE_NAME: {
      name: 'PID_PROFILE_NAME',
      text: 'osdTextElementPidProfileName',
      desc: 'osdDescElementPidProfileName',
      default_position: -1,
      draw_order: 430,
      positionable: true,
      preview: 'PID_1'
    },
    OSD_PROFILE_NAME: {
      name: 'OSD_PROFILE_NAME',
      text: 'osdTextElementOsdProfileName',
      desc: 'osdDescElementOsdProfileName',
      default_position: -1,
      draw_order: 440,
      positionable: true,
      preview: 'OSD_1'
    },
    RSSI_DBM_VALUE: {
      name: 'OSD_PROFILE_NAME',
      text: 'osdTextElementRssiDbmValue',
      desc: 'osdDescElementRssiDbmValue',
      default_position: -1,
      draw_order: 395,
      positionable: true,
      preview: FONT.symbol(SYM.RSSI) + '-130'
    }
  };
};

OSD.constants = {
  VISIBLE: 0x0800,
  VIDEO_TYPES: ['AUTO', 'PAL', 'NTSC'],
  VIDEO_LINES: {
    PAL: 16,
    NTSC: 13
  },
  VIDEO_BUFFER_CHARS: {
    PAL: 480,
    NTSC: 390
  },
  UNIT_TYPES: ['IMPERIAL', 'METRIC'],
  TIMER_PRECISION: ['SECOND', 'HUNDREDTH', 'TENTH'],
  AHISIDEBARWIDTHPOSITION: 7,
  AHISIDEBARHEIGHTPOSITION: 3,
  UNKNOWN_DISPLAY_FIELD: {
    name: 'UNKNOWN',
    text: 'osdTextElementUnknown',
    desc: 'osdDescElementUnknown',
    default_position: -1,
    positionable: true,
    preview: 'UNKNOWN '
  },
  ALL_STATISTIC_FIELDS: {
    MAX_SPEED: {
      name: 'MAX_SPEED',
      text: 'osdTextStatMaxSpeed',
      desc: 'osdDescStatMaxSpeed'
    },
    MIN_BATTERY: {
      name: 'MIN_BATTERY',
      text: 'osdTextStatMinBattery',
      desc: 'osdDescStatMinBattery'
    },
    MIN_RSSI: {
      name: 'MIN_RSSI',
      text: 'osdTextStatMinRssi',
      desc: 'osdDescStatMinRssi'
    },
    MAX_CURRENT: {
      name: 'MAX_CURRENT',
      text: 'osdTextStatMaxCurrent',
      desc: 'osdDescStatMaxCurrent'
    },
    USED_MAH: {
      name: 'USED_MAH',
      text: 'osdTextStatUsedMah',
      desc: 'osdDescStatUsedMah'
    },
    MAX_ALTITUDE: {
      name: 'MAX_ALTITUDE',
      text: 'osdTextStatMaxAltitude',
      desc: 'osdDescStatMaxAltitude'
    },
    BLACKBOX: {
      name: 'BLACKBOX',
      text: 'osdTextStatBlackbox',
      desc: 'osdDescStatBlackbox'
    },
    END_BATTERY: {
      name: 'END_BATTERY',
      text: 'osdTextStatEndBattery',
      desc: 'osdDescStatEndBattery'
    },
    FLYTIME: {
      name: 'FLY_TIME',
      text: 'osdTextStatFlyTime',
      desc: 'osdDescStatFlyTime'
    },
    ARMEDTIME: {
      name: 'ARMED_TIME',
      text: 'osdTextStatArmedTime',
      desc: 'osdDescStatArmedTime'
    },
    MAX_DISTANCE: {
      name: 'MAX_DISTANCE',
      text: 'osdTextStatMaxDistance',
      desc: 'osdDescStatMaxDistance'
    },
    BLACKBOX_LOG_NUMBER: {
      name: 'BLACKBOX_LOG_NUMBER',
      text: 'osdTextStatBlackboxLogNumber',
      desc: 'osdDescStatBlackboxLogNumber'
    },
    TIMER_1: {
      name: 'TIMER_1',
      text: 'osdTextStatTimer1',
      desc: 'osdDescStatTimer1'
    },
    TIMER_2: {
      name: 'TIMER_2',
      text: 'osdTextStatTimer2',
      desc: 'osdDescStatTimer2'
    },
    RTC_DATE_TIME: {
      name: 'RTC_DATE_TIME',
      text: 'osdTextStatRtcDateTime',
      desc: 'osdDescStatRtcDateTime'
    },
    STAT_BATTERY: {
      name: 'BATTERY_VOLTAGE',
      text: 'osdTextStatBattery',
      desc: 'osdDescStatBattery'
    },
    MAX_G_FORCE: {
      name: 'MAX_G_FORCE',
      text: 'osdTextStatGForce',
      desc: 'osdDescStatGForce'
    },
    MAX_ESC_TEMP: {
      name: 'MAX_ESC_TEMP',
      text: 'osdTextStatEscTemperature',
      desc: 'osdDescStatEscTemperature'
    },
    MAX_ESC_RPM: {
      name: 'MAX_ESC_RPM',
      text: 'osdTextStatEscRpm',
      desc: 'osdDescStatEscRpm'
    },
    MIN_LINK_QUALITY: {
      name: 'MIN_LINK_QUALITY',
      text: 'osdTextStatMinLinkQuality',
      desc: 'osdDescStatMinLinkQuality'
    },
    FLIGHT_DISTANCE: {
      name: 'FLIGHT_DISTANCE',
      text: 'osdTextStatFlightDistance',
      desc: 'osdTextStatFlightDistance'
    },
    MAX_FFT: {
      name: 'MAX_FFT',
      text: 'osdTextStatMaxFFT',
      desc: 'osdDescStatMaxFFT'
    },
    TOTAL_FLIGHTS: {
      name: 'TOTAL_FLIGHTS',
      text: 'osdTextStatTotalFlights',
      desc: 'osdDescStatTotalFlights'
    },
    TOTAL_FLIGHT_TIME: {
      name: 'TOTAL_FLIGHT_TIME',
      text: 'osdTextStatTotalFlightTime',
      desc: 'osdDescStatTotalFlightTime'
    },
    TOTAL_FLIGHT_DIST: {
      name: 'TOTAL_FLIGHT_DIST',
      text: 'osdTextStatTotalFlightDistance',
      desc: 'osdDescStatTotalFlightDistance'
    },
    MIN_RSSI_DBM: {
      name: 'MIN_RSSI_DBM',
      text: 'osdTextStatMinRssiDbm',
      desc: 'osdDescStatMinRssiDbm'
    }
  },
  ALL_WARNINGS: {
    ARMING_DISABLED: {
      name: 'ARMING_DISABLED',
      text: 'osdWarningTextArmingDisabled',
      desc: 'osdWarningArmingDisabled'
    },
    BATTERY_NOT_FULL: {
      name: 'BATTERY_NOT_FULL',
      text: 'osdWarningTextBatteryNotFull',
      desc: 'osdWarningBatteryNotFull'
    },
    BATTERY_WARNING: {
      name: 'BATTERY_WARNING',
      text: 'osdWarningTextBatteryWarning',
      desc: 'osdWarningBatteryWarning'
    },
    BATTERY_CRITICAL: {
      name: 'BATTERY_CRITICAL',
      text: 'osdWarningTextBatteryCritical',
      desc: 'osdWarningBatteryCritical'
    },
    VISUAL_BEEPER: {
      name: 'VISUAL_BEEPER',
      text: 'osdWarningTextVisualBeeper',
      desc: 'osdWarningVisualBeeper'
    },
    CRASH_FLIP_MODE: {
      name: 'CRASH_FLIP_MODE',
      text: 'osdWarningTextCrashFlipMode',
      desc: 'osdWarningCrashFlipMode'
    },
    ESC_FAIL: {
      name: 'ESC_FAIL',
      text: 'osdWarningTextEscFail',
      desc: 'osdWarningEscFail'
    },
    CORE_TEMPERATURE: {
      name: 'CORE_TEMPERATURE',
      text: 'osdWarningTextCoreTemperature',
      desc: 'osdWarningCoreTemperature'
    },
    RC_SMOOTHING_FAILURE: {
      name: 'RC_SMOOTHING_FAILURE',
      text: 'osdWarningTextRcSmoothingFailure',
      desc: 'osdWarningRcSmoothingFailure'
    },
    FAILSAFE: {
      name: 'FAILSAFE',
      text: 'osdWarningTextFailsafe',
      desc: 'osdWarningFailsafe'
    },
    LAUNCH_CONTROL: {
      name: 'LAUNCH_CONTROL',
      text: 'osdWarningTextLaunchControl',
      desc: 'osdWarningLaunchControl'
    },
    GPS_RESCUE_UNAVAILABLE: {
      name: 'GPS_RESCUE_UNAVAILABLE',
      text: 'osdWarningTextGpsRescueUnavailable',
      desc: 'osdWarningGpsRescueUnavailable'
    },
    GPS_RESCUE_DISABLED: {
      name: 'GPS_RESCUE_DISABLED',
      text: 'osdWarningTextGpsRescueDisabled',
      desc: 'osdWarningGpsRescueDisabled'
    },
    RSSI: {
      name: 'RSSI',
      text: 'osdWarningTextRSSI',
      desc: 'osdWarningRSSI'
    },
    LINK_QUALITY: {
      name: 'LINK_QUALITY',
      text: 'osdWarningTextLinkQuality',
      desc: 'osdWarningLinkQuality'
    },
    RSSI_DBM: {
      name: 'RSSI_DBM',
      text: 'osdWarningTextRssiDbm',
      desc: 'osdWarningRssiDbm'
    }
  },
  FONT_TYPES: [
    {
      file: 'default',
      name: 'Default'
    }, {
      file: 'bold',
      name: 'Bold'
    }, {
      file: 'large',
      name: 'Large'
    }, {
      file: 'extra_large',
      name: 'Extra Large'
    }, {
      file: 'orniflight',
      name: 'OrniFlight'
    }, {
      file: 'digital',
      name: 'Digital'
    }, {
      file: 'clarity',
      name: 'Clarity'
    }, {
      file: 'vision',
      name: 'Vision'
    }, {
      file: 'impact',
      name: 'Impact'
    }, {
      file: 'impact_mini',
      name: 'Impact Mini'
    }
  ]
};

OSD.searchLimitsElement = function(arrayElements) {
  var limits;
  limits = {
    minX: 0,
    maxX: 0,
    minY: 0,
    maxY: 0
  };
  if (arrayElements.length === 0) {
    return limits;
  }
  if (arrayElements[0].constructor === String) {
    limits.maxY = arrayElements.length;
    limits.minY = 0;
    limits.minX = 0;
    arrayElements.forEach(function(valor, indice, array) {
      limits.maxX = Math.max(valor.length, limits.maxX);
    });
  } else {
    arrayElements.forEach(function(valor, indice, array) {
      limits.minX = Math.min(valor.x, limits.minX);
      limits.maxX = Math.max(valor.x, limits.maxX);
      limits.minY = Math.min(valor.y, limits.minY);
      limits.maxY = Math.max(valor.y, limits.maxY);
    });
  }
  return limits;
};

OSD.chooseFields = function() {
  var F;
  F = OSD.ALL_DISPLAY_FIELDS;
  if (semver.gte(CONFIG.apiVersion, '1.21.0')) {
    OSD.constants.DISPLAY_FIELDS = [F.RSSI_VALUE, F.MAIN_BATT_VOLTAGE, F.CROSSHAIRS, F.ARTIFICIAL_HORIZON, F.HORIZON_SIDEBARS];
    if (semver.lt(CONFIG.apiVersion, '1.36.0')) {
      OSD.constants.DISPLAY_FIELDS = OSD.constants.DISPLAY_FIELDS.concat([F.ONTIME, F.FLYTIME]);
    } else {
      OSD.constants.DISPLAY_FIELDS = OSD.constants.DISPLAY_FIELDS.concat([F.TIMER_1, F.TIMER_2]);
    }
    OSD.constants.DISPLAY_FIELDS = OSD.constants.DISPLAY_FIELDS.concat([F.FLYMODE, F.CRAFT_NAME, F.THROTTLE_POSITION, F.VTX_CHANNEL, F.CURRENT_DRAW, F.MAH_DRAWN, F.GPS_SPEED, F.GPS_SATS, F.ALTITUDE]);
    if (semver.gte(CONFIG.apiVersion, '1.31.0')) {
      OSD.constants.DISPLAY_FIELDS = OSD.constants.DISPLAY_FIELDS.concat([F.PID_ROLL, F.PID_PITCH, F.PID_YAW, F.POWER]);
      if (semver.gte(CONFIG.apiVersion, '1.32.0')) {
        OSD.constants.DISPLAY_FIELDS = OSD.constants.DISPLAY_FIELDS.concat([F.PID_RATE_PROFILE, semver.gte(CONFIG.apiVersion, '1.36.0') ? F.WARNINGS : F.BATTERY_WARNING, F.AVG_CELL_VOLTAGE]);
        if (semver.gte(CONFIG.apiVersion, '1.34.0')) {
          OSD.constants.DISPLAY_FIELDS = OSD.constants.DISPLAY_FIELDS.concat([F.GPS_LON, F.GPS_LAT, F.DEBUG]);
          if (semver.gte(CONFIG.apiVersion, '1.35.0')) {
            OSD.constants.DISPLAY_FIELDS = OSD.constants.DISPLAY_FIELDS.concat([F.PITCH_ANGLE, F.ROLL_ANGLE]);
            if (semver.gte(CONFIG.apiVersion, '1.36.0')) {
              OSD.constants.DISPLAY_FIELDS = OSD.constants.DISPLAY_FIELDS.concat([F.MAIN_BATT_USAGE, F.DISARMED, F.HOME_DIR, F.HOME_DIST, F.NUMERICAL_HEADING, F.NUMERICAL_VARIO, F.COMPASS_BAR, F.ESC_TEMPERATURE, F.ESC_RPM]);
              if (semver.gte(CONFIG.apiVersion, '1.37.0')) {
                OSD.constants.DISPLAY_FIELDS = OSD.constants.DISPLAY_FIELDS.concat([F.REMAINING_TIME_ESTIMATE, F.RTC_DATE_TIME, F.ADJUSTMENT_RANGE, F.CORE_TEMPERATURE]);
                if (semver.gte(CONFIG.apiVersion, '1.39.0')) {
                  OSD.constants.DISPLAY_FIELDS = OSD.constants.DISPLAY_FIELDS.concat([F.ANTI_GRAVITY]);
                  if (semver.gte(CONFIG.apiVersion, '1.40.0')) {
                    OSD.constants.DISPLAY_FIELDS = OSD.constants.DISPLAY_FIELDS.concat([F.G_FORCE]);
                    if (semver.gte(CONFIG.apiVersion, '1.41.0')) {
                      OSD.constants.DISPLAY_FIELDS = OSD.constants.DISPLAY_FIELDS.concat([F.MOTOR_DIAG, F.LOG_STATUS, F.FLIP_ARROW, F.LINK_QUALITY, F.FLIGHT_DIST, F.STICK_OVERLAY_LEFT, F.STICK_OVERLAY_RIGHT, F.DISPLAY_NAME, F.ESC_RPM_FREQ]);
                      if (semver.gte(CONFIG.apiVersion, '1.42.0')) {
                        OSD.constants.DISPLAY_FIELDS = OSD.constants.DISPLAY_FIELDS.concat([F.RATE_PROFILE_NAME, F.PID_PROFILE_NAME, F.OSD_PROFILE_NAME, F.RSSI_DBM_VALUE]);
                      }
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
  } else {
    OSD.constants.DISPLAY_FIELDS = [F.MAIN_BATT_VOLTAGE, F.RSSI_VALUE, F.TIMER, F.THROTTLE_POSITION, F.CPU_LOAD, F.VTX_CHANNEL, F.VOLTAGE_WARNING, F.ARMED, F.DISARMED, F.ARTIFICIAL_HORIZON, F.HORIZON_SIDEBARS, F.CURRENT_DRAW, F.MAH_DRAWN, F.CRAFT_NAME, F.ALTITUDE];
  }
  F = OSD.constants.ALL_STATISTIC_FIELDS;
  if (semver.lt(CONFIG.apiVersion, '1.39.0')) {
    OSD.constants.STATISTIC_FIELDS = [F.MAX_SPEED, F.MIN_BATTERY, F.MIN_RSSI, F.MAX_CURRENT, F.USED_MAH, F.MAX_ALTITUDE, F.BLACKBOX, F.END_BATTERY, F.TIMER_1, F.TIMER_2, F.MAX_DISTANCE, F.BLACKBOX_LOG_NUMBER];
    if (semver.gte(CONFIG.apiVersion, '1.37.0')) {
      OSD.constants.STATISTIC_FIELDS = OSD.constants.STATISTIC_FIELDS.concat([F.RTC_DATE_TIME]);
    }
  } else {
    OSD.constants.STATISTIC_FIELDS = [F.RTC_DATE_TIME, F.TIMER_1, F.TIMER_2, F.MAX_SPEED, F.MAX_DISTANCE, F.MIN_BATTERY, F.END_BATTERY, F.STAT_BATTERY, F.MIN_RSSI, F.MAX_CURRENT, F.USED_MAH, F.MAX_ALTITUDE, F.BLACKBOX, F.BLACKBOX_LOG_NUMBER];
    if (semver.gte(CONFIG.apiVersion, '1.41.0')) {
      OSD.constants.STATISTIC_FIELDS = OSD.constants.STATISTIC_FIELDS.concat([F.MAX_G_FORCE, F.MAX_ESC_TEMP, F.MAX_ESC_RPM, F.MIN_LINK_QUALITY, F.FLIGHT_DISTANCE, F.MAX_FFT]);
    }
    if (semver.gte(CONFIG.apiVersion, '1.42.0')) {
      OSD.constants.STATISTIC_FIELDS = OSD.constants.STATISTIC_FIELDS.concat([F.TOTAL_FLIGHTS, F.TOTAL_FLIGHT_TIME, F.TOTAL_FLIGHT_DIST, F.MIN_RSSI_DBM]);
    }
  }
  F = OSD.constants.ALL_WARNINGS;
  OSD.constants.WARNINGS = [F.ARMING_DISABLED, F.BATTERY_NOT_FULL, F.BATTERY_WARNING, F.BATTERY_CRITICAL, F.VISUAL_BEEPER, F.CRASH_FLIP_MODE];
  if (semver.gte(CONFIG.apiVersion, '1.39.0')) {
    OSD.constants.WARNINGS = OSD.constants.WARNINGS.concat([F.ESC_FAIL, F.CORE_TEMPERATURE, F.RC_SMOOTHING_FAILURE]);
  }
  if (semver.gte(CONFIG.apiVersion, '1.41.0')) {
    OSD.constants.WARNINGS = OSD.constants.WARNINGS.concat([F.FAILSAFE, F.LAUNCH_CONTROL, F.GPS_RESCUE_UNAVAILABLE, F.GPS_RESCUE_DISABLED]);
  }
  OSD.constants.TIMER_TYPES = ['ON_TIME', 'TOTAL_ARMED_TIME', 'LAST_ARMED_TIME'];
  if (semver.gte(CONFIG.apiVersion, '1.42.0')) {
    OSD.constants.TIMER_TYPES = OSD.constants.TIMER_TYPES.concat(['ON_ARM_TIME']);
    OSD.constants.WARNINGS = OSD.constants.WARNINGS.concat([F.RSSI, F.LINK_QUALITY, F.RSSI_DBM]);
  }
};

OSD.updateDisplaySize = function() {
  var video_type;
  video_type = OSD.constants.VIDEO_TYPES[OSD.data.video_system];
  if (video_type === 'AUTO') {
    video_type = 'PAL';
  }
  OSD.data.display_size = {
    x: FONT.constants.SIZES.LINE,
    y: OSD.constants.VIDEO_LINES[video_type],
    total: null
  };
};

OSD.drawByOrder = function(selectedPosition, field, charCode, x, y) {
  var oldField;
  if (OSD.data.preview[selectedPosition] !== void 0) {
    oldField = OSD.data.preview[selectedPosition][0];
    if (oldField !== null) {
      if (oldField.draw_order !== void 0) {
        if (field.draw_order === void 0 || field.draw_order < oldField.draw_order) {
          return;
        }
      }
    }
    OSD.data.preview[selectedPosition++] = [field, charCode, x, y];
  }
};

OSD.msp = {
  helpers: {
    unpack: {
      position: function(bits, c) {
        var default_position, display_item, osd_profile, positionable;
        display_item = {};
        positionable = typeof c.positionable === 'function' ? c.positionable() : c.positionable;
        default_position = typeof c.default_position === 'function' ? c.default_position() : c.default_position;
        display_item.positionable = positionable;
        if (semver.gte(CONFIG.apiVersion, '1.21.0')) {
          display_item.position = positionable ? FONT.constants.SIZES.LINE * (bits >> 5 & 0x001F) + (bits & 0x001F) : default_position;
          display_item.isVisible = [];
          osd_profile = 0;
          while (osd_profile < OSD.getNumberOfProfiles()) {
            display_item.isVisible[osd_profile] = (bits & OSD.constants.VISIBLE << osd_profile) !== 0;
            osd_profile++;
          }
        } else {
          display_item.position = bits === -1 ? default_position : bits;
          display_item.isVisible = [bits !== -1];
        }
        return display_item;
      },
      timer: function(bits, c) {
        var timer;
        timer = {
          src: bits & 0x0F,
          precision: bits >> 4 & 0x0F,
          alarm: bits >> 8 & 0xFF
        };
        return timer;
      }
    },
    pack: {
      position: function(display_item) {
        var isVisible, osd_profile, packed_visible, position;
        isVisible = display_item.isVisible;
        position = display_item.position;
        if (semver.gte(CONFIG.apiVersion, '1.21.0')) {
          packed_visible = 0;
          osd_profile = 0;
          while (osd_profile < OSD.getNumberOfProfiles()) {
            packed_visible |= isVisible[osd_profile] ? OSD.constants.VISIBLE << osd_profile : 0;
            osd_profile++;
          }
          return packed_visible | (position / FONT.constants.SIZES.LINE & 0x001F) << 5 | position % FONT.constants.SIZES.LINE;
        } else {
          if (isVisible[0]) {
            if (position === -1) {
              return 0;
            } else {
              return position;
            }
          } else {
            return -1;
          }
        }
      },
      timer: function(_timer) {
        return _timer.src & 0x0F | (_timer.precision & 0x0F) << 4 | (_timer.alarm & 0xFF) << 8;
      }
    }
  },
  encodeOther: function() {
    var i, result, warningFlags;
    result = [-1, OSD.data.video_system];
    if (OSD.data.state.haveOsdFeature && semver.gte(CONFIG.apiVersion, '1.21.0')) {
      result.push8(OSD.data.unit_mode);
      result.push8(OSD.data.alarms.rssi.value);
      result.push16(OSD.data.alarms.cap.value);
      if (semver.lt(CONFIG.apiVersion, '1.36.0')) {
        result.push16(OSD.data.alarms.time.value);
      } else {
        result.push16(0);
      }
      result.push16(OSD.data.alarms.alt.value);
      if (semver.gte(CONFIG.apiVersion, '1.37.0')) {
        warningFlags = 0;
        i = 0;
        while (i < OSD.data.warnings.length) {
          if (OSD.data.warnings[i].enabled) {
            warningFlags |= 1 << i;
          }
          i++;
        }
        console.log(warningFlags);
        result.push16(warningFlags);
        if (semver.gte(CONFIG.apiVersion, '1.41.0')) {
          result.push32(warningFlags);
          result.push8(OSD.data.osd_profiles.selected + 1);
        }
      }
    }
    return result;
  },
  encodeLayout: function(display_item) {
    var buffer;
    buffer = [];
    buffer.push8(display_item.index);
    buffer.push16(this.helpers.pack.position(display_item));
    return buffer;
  },
  encodeStatistics: function(stat_item) {
    var buffer;
    buffer = [];
    buffer.push8(stat_item.index);
    buffer.push16(stat_item.enabled);
    buffer.push8(0);
    return buffer;
  },
  encodeTimer: function(timer) {
    var buffer;
    buffer = [-2, timer.index];
    buffer.push16(this.helpers.pack.timer(timer));
    return buffer;
  },
  decode: function(payload) {
    var v;
    var i;
    var j;
    var _c, _item, _iterator, _j, _step, _v, c, d, displayItemsCountActual, err, expectedStatsCount, expectedTimersCount, i, ignoreSize, item, items_positions_read, j, statisticNumber, suffix, tmp, v, view, warningCount, warningFlags, warningNumber;
    view = payload.data;
    d = OSD.data;
    displayItemsCountActual = OSD.constants.DISPLAY_FIELDS.length;
    d.flags = view.readU8();
    if (d.flags > 0) {
      if (payload.length > 1) {
        d.video_system = view.readU8();
        if (semver.gte(CONFIG.apiVersion, '1.21.0') && bit_check(d.flags, 0)) {
          d.unit_mode = view.readU8();
          d.alarms = {};
          d.alarms['rssi'] = {
            display_name: i18n.getMessage('osdTimerAlarmOptionRssi'),
            value: view.readU8()
          };
          d.alarms['cap'] = {
            display_name: i18n.getMessage('osdTimerAlarmOptionCapacity'),
            value: view.readU16()
          };
          if (semver.lt(CONFIG.apiVersion, '1.36.0')) {
            d.alarms['time'] = {
              display_name: 'Minutes',
              value: view.readU16()
            };
          } else {
            view.readU8();
            tmp = view.readU8();
            if (semver.gte(CONFIG.apiVersion, '1.37.0')) {
              displayItemsCountActual = tmp;
            }
          }
          d.alarms['alt'] = {
            display_name: i18n.getMessage('osdTimerAlarmOptionAltitude'),
            value: view.readU16()
          };
        }
      }
    }
    d.state = {};
    d.state.haveSomeOsd = d.flags !== 0;
    d.state.haveMax7456Video = bit_check(d.flags, 4) || d.flags === 1 && semver.lt(CONFIG.apiVersion, '1.34.0');
    d.state.haveOsdFeature = bit_check(d.flags, 0) || d.flags === 1 && semver.lt(CONFIG.apiVersion, '1.34.0');
    d.state.isOsdSlave = bit_check(d.flags, 1) && semver.gte(CONFIG.apiVersion, '1.34.0');
    d.display_items = [];
    d.stat_items = [];
    d.warnings = [];
    d.timers = [];
    items_positions_read = [];
    while (view.offset < view.byteLength && items_positions_read.length < displayItemsCountActual) {
      v = null;
      if (semver.gte(CONFIG.apiVersion, '1.21.0')) {
        v = view.readU16();
      } else {
        v = view.read16();
      }
      items_positions_read.push(v);
    }
    if (semver.gte(CONFIG.apiVersion, '1.36.0')) {
      expectedStatsCount = view.readU8();
      if (expectedStatsCount !== OSD.constants.STATISTIC_FIELDS.length) {
        console.error('Firmware is transmitting a different number of statistics (' + expectedStatsCount + ') to what the configurator is expecting (' + OSD.constants.STATISTIC_FIELDS.length + ')');
      }
      i = 0;
      while (i < expectedStatsCount) {
        _v = view.readU8();
        if (i < OSD.constants.STATISTIC_FIELDS.length) {
          _c = OSD.constants.STATISTIC_FIELDS[i];
          d.stat_items.push({
            name: _c.name,
            text: _c.text,
            desc: _c.desc,
            index: i,
            enabled: _v === 1
          });
        } else {
          statisticNumber = i - OSD.constants.STATISTIC_FIELDS.length + 1;
          d.stat_items.push({
            name: 'UNKNOWN',
            text: ['osdTextStatUnknown', statisticNumber],
            desc: 'osdDescStatUnknown',
            index: i,
            enabled: _v === 1
          });
        }
        i++;
      }
      expectedTimersCount = view.readU8();
      while (view.offset < view.byteLength && expectedTimersCount > 0) {
        v = view.readU16();
        j = d.timers.length;
        d.timers.push($.extend({
          index: j
        }, this.helpers.unpack.timer(v, c)));
        expectedTimersCount--;
      }
      while (expectedTimersCount > 0) {
        view.readU16();
        expectedTimersCount--;
      }
      warningCount = OSD.constants.WARNINGS.length;
      warningFlags = view.readU16();
      if (semver.gte(CONFIG.apiVersion, '1.41.0')) {
        warningCount = view.readU8();
        warningFlags = view.readU32();
      }
      i = 0;
      while (i < warningCount) {
        if (i < OSD.constants.WARNINGS.length) {
          d.warnings.push($.extend(OSD.constants.WARNINGS[i], {
            enabled: (warningFlags & 1 << i) !== 0
          }));
        } else {
          warningNumber = i - OSD.constants.WARNINGS.length + 1;
          d.warnings.push({
            name: 'UNKNOWN',
            text: ['osdWarningTextUnknown', warningNumber],
            desc: 'osdWarningUnknown',
            enabled: (warningFlags & 1 << i) !== 0
          });
        }
        i++;
      }
    }
    if (semver.gte(CONFIG.apiVersion, '1.41.0')) {
      d.osd_profiles.number = view.readU8();
      d.osd_profiles.selected = view.readU8() - 1;
    } else {
      d.osd_profiles.number = 1;
      d.osd_profiles.selected = 0;
    }
    _j = 0;
    while (_j < items_positions_read.length) {
      item = items_positions_read[_j];
      j = d.display_items.length;
      c = void 0;
      suffix = void 0;
      ignoreSize = false;
      if (d.display_items.length < OSD.constants.DISPLAY_FIELDS.length) {
        c = OSD.constants.DISPLAY_FIELDS[j];
      } else {
        c = OSD.constants.UNKNOWN_DISPLAY_FIELD;
        suffix = '' + 1 + d.display_items.length - OSD.constants.DISPLAY_FIELDS.length;
        ignoreSize = true;
      }
      d.display_items.push($.extend({
        name: c.name,
        text: suffix ? [c.text, suffix] : c.text,
        desc: c.desc,
        index: j,
        draw_order: c.draw_order,
        preview: suffix ? c.preview + suffix : c.preview,
        ignoreSize: ignoreSize
      }, this.helpers.unpack.position(item, c)));
      _j++;
    }
    _iterator = _createForOfIteratorHelper(d.display_items);
    _step = void 0;
    try {
      _iterator.s();
      while (!(_step = _iterator.n()).done) {
        _item = _step.value;
        if (typeof _item.preview === 'function') {
          _item.preview = _item.preview(d);
        }
      }
    } catch (error1) {
      err = error1;
      _iterator.e(err);
    } finally {
      _iterator.f();
    }
    OSD.updateDisplaySize();
  }
};

OSD.GUI = {};

OSD.GUI.preview = {
  onMouseEnter: function() {
    if (!$(this).data('field')) {
      return;
    }
    $('#element-fields .field-' + $(this).data('field').index).addClass('mouseover');
  },
  onMouseLeave: function() {
    if (!$(this).data('field')) {
      return;
    }
    $('#element-fields .field-' + $(this).data('field').index).removeClass('mouseover');
  },
  onDragStart: function(e) {
    var arrayElements, display_item, ev, limits, offsetX, offsetY, xPos, yPos;
    ev = e.originalEvent;
    display_item = OSD.data.display_items[$(ev.target).data('field').index];
    xPos = ev.currentTarget.dataset.x;
    yPos = ev.currentTarget.dataset.y;
    offsetX = 6;
    offsetY = 9;
    if (display_item.preview.constructor === Array) {
      arrayElements = display_item.preview;
      limits = OSD.searchLimitsElement(arrayElements);
      xPos -= limits.minX;
      yPos -= limits.minY;
      offsetX += xPos * 12;
      offsetY += yPos * 18;
    }
    ev.dataTransfer.setData('text/plain', $(ev.target).data('field').index);
    ev.dataTransfer.setData('x', ev.currentTarget.dataset.x);
    ev.dataTransfer.setData('y', ev.currentTarget.dataset.y);
    ev.dataTransfer.setDragImage($(this).data('field').preview_img, offsetX, offsetY);
  },
  onDragOver: function(e) {
    var ev;
    ev = e.originalEvent;
    ev.preventDefault();
    ev.dataTransfer.dropEffect = 'move';
    $(this).css({
      background: 'rgba(0,0,0,.5)'
    });
  },
  onDragLeave: function(e) {
    $(this).removeAttr('style');
  },
  onDrop: function(e) {
    var arrayElements, cursor, cursorX, display_item, ev, field_id, limits, overflows_line, position, selectedPositionX, selectedPositionY, x, y;
    ev = e.originalEvent;
    field_id = parseInt(ev.dataTransfer.getData('text/plain'));
    display_item = OSD.data.display_items[field_id];
    position = $(this).removeAttr('style').data('position');
    cursor = position;
    cursorX = cursor % FONT.constants.SIZES.LINE;
    if (display_item.preview.constructor === Array) {
      console.log('Initial Drop Position: ' + position);
      x = parseInt(ev.dataTransfer.getData('x'));
      y = parseInt(ev.dataTransfer.getData('y'));
      console.log('XY Co-ords:' + x + '-' + y);
      position -= x;
      position -= y * FONT.constants.SIZES.LINE;
      console.log('Calculated Position: ' + position);
    }
    if (!display_item.ignoreSize) {
      if (display_item.preview.constructor !== Array) {
        overflows_line = FONT.constants.SIZES.LINE - (position % FONT.constants.SIZES.LINE + display_item.preview.length);
        if (overflows_line < 0) {
          position += overflows_line;
        }
      } else {
        arrayElements = display_item.preview;
        limits = OSD.searchLimitsElement(arrayElements);
        selectedPositionX = position % FONT.constants.SIZES.LINE;
        selectedPositionY = Math.trunc(position / FONT.constants.SIZES.LINE);
        if (arrayElements[0].constructor === String) {
          if (position < 0) {
            return;
          }
          if (selectedPositionX > cursorX) {
            position += FONT.constants.SIZES.LINE - selectedPositionX;
            selectedPositionY++;
          } else if (selectedPositionX + limits.maxX > FONT.constants.SIZES.LINE) {
            position -= selectedPositionX + limits.maxX - FONT.constants.SIZES.LINE;
          }
          if (selectedPositionY < 0) {
            position += Math.abs(selectedPositionY) * FONT.constants.SIZES.LINE;
          } else if (selectedPositionY + limits.maxY > OSD.data.display_size.y) {
            position -= (selectedPositionY + limits.maxY - OSD.data.display_size.y) * FONT.constants.SIZES.LINE;
          }
        } else {
          if (limits.minX < 0 && selectedPositionX + limits.minX < 0) {
            position += Math.abs(selectedPositionX + limits.minX);
          } else if (limits.maxX > 0 && selectedPositionX + limits.maxX >= FONT.constants.SIZES.LINE) {
            position -= selectedPositionX + limits.maxX + 1 - FONT.constants.SIZES.LINE;
          }
          if (limits.minY < 0 && selectedPositionY + limits.minY < 0) {
            position += Math.abs(selectedPositionY + limits.minY) * FONT.constants.SIZES.LINE;
          } else if (limits.maxY > 0 && selectedPositionY + limits.maxY >= OSD.data.display_size.y) {
            position -= (selectedPositionY + limits.maxY - OSD.data.display_size.y + 1) * FONT.constants.SIZES.LINE;
          }
        }
      }
    }
    if (semver.gte(CONFIG.apiVersion, '1.21.0')) {

    } else {
      if (position > OSD.data.display_size.total / 2) {
        position = position - OSD.data.display_size.total;
      }
    }
    $('input.' + field_id + '.position').val(position).change();
  }
};

TABS.osd = {};

TABS.osd.initialize = function(callback) {
  var self;
  self = this;
  if (GUI.active_tab !== 'osd') {
    GUI.active_tab = 'osd';
  }
  $('#content').load('./tabs/osd.html', function() {
    var fontPresetsElement, fontPreviewElement, fontbuttons, insertOrdered, titleizeField, updateOsdView;
    titleizeField = function(field) {
      var finalFieldName;
      finalFieldName = null;
      if (field.text) {
        if (Array.isArray(field.text) && i18n.existsMessage(field.text[0])) {
          finalFieldName = i18n.getMessage(field.text[0], field.text.slice(1));
        } else {
          finalFieldName = i18n.getMessage(field.text);
        }
      }
      return finalFieldName;
    };
    insertOrdered = function(fieldList, field) {
      var added, currentLocale;
      if (field.name === 'UNKNOWN') {
        fieldList.append(field);
      } else {
        added = false;
        currentLocale = i18n.getCurrentLocale().replace('_', '-');
        fieldList.children().each(function() {
          if ($(this).text().localeCompare(field.text(), currentLocale, {
            sensitivity: 'base'
          }) > 0) {
            $(this).before(field);
            added = true;
            return false;
          }
        });
        if (!added) {
          fieldList.append(field);
        }
      }
    };
    updateOsdView = function() {
      MSP.promise(MSPCodes.MSP_OSD_CONFIG).then(function(info) {
        var i;
        var type;
        var $checkbox;
        var alarm;
        var $field;
        var desc;
        var $field;
        var desc;
        var i;
        var i;
        var i;
        var charCode;
        var img;
        var charCode;
        var img;
        var i;
        var charCode;
        var charCode;
        var $alarms, $checkbox, $displayFields, $field, $img, $input, $preview, $row, $statsFields, $timerConfig, $timers, $unitMode, $videoTypes, $warningFields, _field, _field2, _field3, _field4, _field5, _finalFieldName, _iterator2, _iterator3, _iterator4, _iterator5, _iterator6, _iterator7, _iterator8, _profileNumber, _step2, _step3, _step4, _step5, _step6, _step7, _step8, alarm, alarmInput, alarmTimerTableData, arrayElements, canvas, charCode, ctx, desc, element, enabledCount, err, field, finalFieldName, headerSwitches_e, i, img, j, k, limits, numberOfProfiles, offsetX, offsetY, option, optionText, osdFontPresetsSelector_e, osdFontSelector_e, osdProfileActive_e, osdProfileSelector_e, osd_profile, precision, precisionTimerTableData, profileNumber, selectedPosition, sourceTimerTableData, src, tim, timerTable, timerTableRow, tt, type, x, y;
        OSD.chooseFields();
        OSD.msp.decode(info);
        if (OSD.data.state.haveSomeOsd === 0) {
          $('.unsupported').fadeIn();
          return;
        }
        $('.supported').fadeIn();
        $videoTypes = $('.video-types').empty();
        i = 0;
        while (i < OSD.constants.VIDEO_TYPES.length) {
          type = OSD.constants.VIDEO_TYPES[i];
          $checkbox = $('<label/>').append($('<input name="video_system" type="radio"/>' + i18n.getMessage('osdSetupVideoFormatOption' + inflection.camelize(type.toLowerCase())) + '</label>').prop('checked', i === OSD.data.video_system).data('type', type).data('type', i));
          $videoTypes.append($checkbox);
          i++;
        }
        $videoTypes.find(':radio').click(function(e) {
          OSD.data.video_system = $(this).data('type');
          MSP.promise(MSPCodes.MSP_SET_OSD_CONFIG, OSD.msp.encodeOther()).then(function() {
            updateOsdView();
          });
        });
        if (semver.gte(CONFIG.apiVersion, '1.21.0')) {
          $('.units-container').show();
          $unitMode = $('.units').empty();
          i = 0;
          while (i < OSD.constants.UNIT_TYPES.length) {
            type = OSD.constants.UNIT_TYPES[i];
            $checkbox = $('<label/>').append($('<input name="unit_mode" type="radio"/>' + i18n.getMessage('osdSetupUnitsOption' + inflection.camelize(type.toLowerCase())) + '</label>').prop('checked', i === OSD.data.unit_mode).data('type', type).data('type', i));
            $unitMode.append($checkbox);
            i++;
          }
          $unitMode.find(':radio').click(function(e) {
            OSD.data.unit_mode = $(this).data('type');
            MSP.promise(MSPCodes.MSP_SET_OSD_CONFIG, OSD.msp.encodeOther()).then(function() {
              updateOsdView();
            });
          });
          $('.alarms-container').show();
          $alarms = $('.alarms').empty();
          for (k in OSD.data.alarms) {
            alarm = OSD.data.alarms[k];
            alarmInput = $('<input name="alarm" type="number" id="' + k + '"/>' + alarm.display_name + '</label>');
            alarmInput.val(alarm.value);
            alarmInput.focusout(function(e) {
              OSD.data.alarms[$(this)[0].id].value = $(this)[0].value;
              MSP.promise(MSPCodes.MSP_SET_OSD_CONFIG, OSD.msp.encodeOther()).then(function() {
                updateOsdView();
              });
            });
            $input = $('<label/>').append(alarmInput);
            $alarms.append($input);
          }
          if (semver.gte(CONFIG.apiVersion, '1.36.0')) {
            $('.timers-container').show();
            $timers = $('#timer-fields').empty();
            _iterator2 = _createForOfIteratorHelper(OSD.data.timers);
            _step2 = void 0;
            try {
              _iterator2.s();
              while (!(_step2 = _iterator2.n()).done) {
                tim = _step2.value;
                $timerConfig = $('<div class="switchable-field field-' + tim.index + '"/>');
                timerTable = $('<table />');
                $timerConfig.append(timerTable);
                timerTableRow = $('<tr />');
                timerTable.append(timerTableRow);
                timerTableRow.append('<td>' + tim.index + 1 + '</td>');
                sourceTimerTableData = $('<td class="osd_tip"></td>');
                sourceTimerTableData.attr('title', i18n.getMessage('osdTimerSourceTooltip'));
                sourceTimerTableData.append('<label for="timerSource_' + tim.index + '" class="char-label">' + i18n.getMessage('osdTimerSource') + '</label>');
                src = $('<select class="timer-option" id="timerSource_' + tim.index + '"></select>');
                OSD.constants.TIMER_TYPES.forEach(function(e, i) {
                  src.append('<option value="' + i + '">' + i18n.getMessage('osdTimerSourceOption' + inflection.camelize(e.toLowerCase())) + '</option>');
                });
                src[0].selectedIndex = tim.src;
                src.blur(function(e) {
                  var idx;
                  idx = $(this)[0].id.split('_')[1];
                  OSD.data.timers[idx].src = $(this)[0].selectedIndex;
                  MSP.promise(MSPCodes.MSP_SET_OSD_CONFIG, OSD.msp.encodeTimer(OSD.data.timers[idx])).then(function() {
                    updateOsdView();
                  });
                });
                sourceTimerTableData.append(src);
                timerTableRow.append(sourceTimerTableData);
                timerTableRow = $('<tr />');
                timerTable.append(timerTableRow);
                precisionTimerTableData = $('<td class="osd_tip"></td>');
                precisionTimerTableData.attr('title', i18n.getMessage('osdTimerPrecisionTooltip'));
                precisionTimerTableData.append('<label for="timerPrec_' + tim.index + '" class="char-label">' + i18n.getMessage('osdTimerPrecision') + '</label>');
                precision = $('<select class="timer-option osd_tip" id="timerPrec_' + tim.index + '"></select>');
                OSD.constants.TIMER_PRECISION.forEach(function(e, i) {
                  precision.append('<option value="' + i + '">' + i18n.getMessage('osdTimerPrecisionOption' + inflection.camelize(e.toLowerCase())) + '</option>');
                });
                precision[0].selectedIndex = tim.precision;
                precision.blur(function(e) {
                  var idx;
                  idx = $(this)[0].id.split('_')[1];
                  OSD.data.timers[idx].precision = $(this)[0].selectedIndex;
                  MSP.promise(MSPCodes.MSP_SET_OSD_CONFIG, OSD.msp.encodeTimer(OSD.data.timers[idx])).then(function() {
                    updateOsdView();
                  });
                });
                precisionTimerTableData.append(precision);
                timerTableRow.append('<td></td>');
                timerTableRow.append(precisionTimerTableData);
                timerTableRow = $('<tr />');
                timerTable.append(timerTableRow);
                alarmTimerTableData = $('<td class="osd_tip"></td>');
                alarmTimerTableData.attr('title', i18n.getMessage('osdTimerAlarmTooltip'));
                alarmTimerTableData.append('<label for="timerAlarm_' + tim.index + '" class="char-label">' + i18n.getMessage('osdTimerAlarm') + '</label>');
                alarm = $('<input class="timer-option osd_tip" name="alarm" type="number" min=0 id="timerAlarm_' + tim.index + '"/>');
                alarm[0].value = tim.alarm;
                alarm.blur(function(e) {
                  var idx;
                  idx = $(this)[0].id.split('_')[1];
                  OSD.data.timers[idx].alarm = $(this)[0].value;
                  MSP.promise(MSPCodes.MSP_SET_OSD_CONFIG, OSD.msp.encodeTimer(OSD.data.timers[idx])).then(function() {
                    updateOsdView();
                  });
                });
                alarmTimerTableData.append(alarm);
                timerTableRow.append('<td></td>');
                timerTableRow.append(alarmTimerTableData);
                $timers.append($timerConfig);
              }
            } catch (error1) {
              err = error1;
              _iterator2.e(err);
            } finally {
              _iterator2.f();
            }
            $('.stats-container').show();
            $statsFields = $('#post-flight-stat-fields').empty();
            _iterator3 = _createForOfIteratorHelper(OSD.data.stat_items);
            _step3 = void 0;
            try {
              _iterator3.s();
              while (!(_step3 = _iterator3.n()).done) {
                _field = _step3.value;
                if (!_field.name) {
                  continue;
                }
                $field = $('<div class="switchable-field field-' + _field.index + '"/>');
                desc = null;
                if (_field.desc && _field.desc.length) {
                  desc = i18n.getMessage(_field.desc);
                }
                if (desc && desc.length) {
                  $field[0].classList.add('osd_tip');
                  $field.attr('title', desc);
                }
                $field.append($('<input type="checkbox" name="' + _field.name + '" class="togglesmall"></input>').data('field', _field).attr('checked', _field.enabled).change(function(e) {
                  var field;
                  field = $(this).data('field');
                  field.enabled = !field.enabled;
                  MSP.promise(MSPCodes.MSP_SET_OSD_CONFIG, OSD.msp.encodeStatistics(field)).then(function() {
                    updateOsdView();
                  });
                }));
                $field.append('<label for="' + _field.name + '" class="char-label">' + titleizeField(_field) + '</label>');
                insertOrdered($statsFields, $field);
              }
            } catch (error1) {
              err = error1;
              _iterator3.e(err);
            } finally {
              _iterator3.f();
            }
            $('.warnings-container').show();
            $warningFields = $('#warnings-fields').empty();
            _iterator4 = _createForOfIteratorHelper(OSD.data.warnings);
            _step4 = void 0;
            try {
              _iterator4.s();
              while (!(_step4 = _iterator4.n()).done) {
                _field2 = _step4.value;
                if (!_field2.name) {
                  continue;
                }
                $field = $('<div class="switchable-field field-' + _field2.index + '"/>');
                desc = null;
                if (_field2.desc && _field2.desc.length) {
                  desc = i18n.getMessage(_field2.desc);
                }
                if (desc && desc.length) {
                  $field[0].classList.add('osd_tip');
                  $field.attr('title', desc);
                }
                $field.append($('<input type="checkbox" name="' + _field2.name + '" class="togglesmall"></input>').data('field', _field2).attr('checked', _field2.enabled).change(function(e) {
                  var field;
                  field = $(this).data('field');
                  field.enabled = !field.enabled;
                  MSP.promise(MSPCodes.MSP_SET_OSD_CONFIG, OSD.msp.encodeOther()).then(function() {
                    updateOsdView();
                  });
                }));
                finalFieldName = titleizeField(_field2);
                $field.append('<label for="' + _field2.name + '" class="char-label">' + finalFieldName + '</label>');
                insertOrdered($warningFields, $field);
              }
            } catch (error1) {
              err = error1;
              _iterator4.e(err);
            } finally {
              _iterator4.f();
            }
          }
        }
        if (!OSD.data.state.haveMax7456Video) {
          $('.requires-max7456').hide();
        }
        if (!OSD.data.state.haveOsdFeature) {
          $('.requires-osd-feature').hide();
        }
        numberOfProfiles = OSD.getNumberOfProfiles();
        headerSwitches_e = $('.elements').find('.osd-profiles-header');
        if (headerSwitches_e.children().length === 0) {
          profileNumber = 0;
          while (profileNumber < numberOfProfiles) {
            headerSwitches_e.append('<span class="profileOsdHeader">' + profileNumber + 1 + '</span>');
            profileNumber++;
          }
        }
        osdProfileSelector_e = $('.osdprofile-selector');
        osdProfileActive_e = $('.osdprofile-active');
        if (osdProfileSelector_e.children().length === 0) {
          _profileNumber = 0;
          while (_profileNumber < numberOfProfiles) {
            optionText = i18n.getMessage('osdSetupPreviewSelectProfileElement', {
              profileNumber: _profileNumber + 1
            });
            osdProfileSelector_e.append(new Option(optionText, _profileNumber));
            osdProfileActive_e.append(new Option(optionText, _profileNumber));
            _profileNumber++;
          }
        }
        osdProfileActive_e.val(OSD.data.osd_profiles.selected);
        osdFontSelector_e = $('.osdfont-selector');
        osdFontPresetsSelector_e = $('.fontpresets');
        if (osdFontSelector_e.children().length === 0) {
          option = $('<option>', {
            text: i18n.getMessage('osdSetupFontPresetsSelectorCustomOption'),
            value: -1,
            'disabled': 'disabled',
            'style': 'display: none;'
          });
          osdFontSelector_e.append($(option));
          OSD.constants.FONT_TYPES.forEach(function(e, i) {
            var optionText;
            optionText = i18n.getMessage('osdSetupPreviewSelectFontElement', {
              fontName: e.name
            });
            osdFontSelector_e.append(new Option(optionText, e.file));
          });
          osdFontSelector_e.change(function() {
            osdFontPresetsSelector_e.val(this.value).change();
          });
        }
        osdFontSelector_e.val(osdFontPresetsSelector_e.val() !== null ? osdFontPresetsSelector_e.val() : -1);
        $('.osdfont-selector option[value=-1]').toggle(osdFontSelector_e.val() === -1);
        $displayFields = $('#element-fields').empty();
        enabledCount = 0;
        _iterator5 = _createForOfIteratorHelper(OSD.data.display_items);
        _step5 = void 0;
        try {
          _iterator5.s();
          while (!(_step5 = _iterator5.n()).done) {
            _field3 = _step5.value;
            if (!_field3.name) {
              continue;
            }
            if (_field3.isVisible[OSD.getCurrentPreviewProfile()]) {
              enabledCount++;
            }
            $field = $('<div class="switchable-field field-' + _field3.index + '"/>');
            desc = null;
            if (_field3.desc && _field3.desc.length) {
              desc = i18n.getMessage(_field3.desc);
            }
            if (desc && desc.length) {
              $field[0].classList.add('osd_tip');
              $field.attr('title', desc);
            }
            osd_profile = 0;
            while (osd_profile < OSD.getNumberOfProfiles()) {
              $field.append($('<input type="checkbox" name="' + _field3.name + '"></input>').data('field', _field3).data('osd_profile', osd_profile).attr('checked', _field3.isVisible[osd_profile]).change(function(e) {
                var $position, field, profile;
                field = $(this).data('field');
                profile = $(this).data('osd_profile');
                $position = $(this).parent().find('.position.' + field.name);
                field.isVisible[profile] = !field.isVisible[profile];
                if (field.isVisible[OSD.getCurrentPreviewProfile()]) {
                  $position.show();
                } else {
                  $position.hide();
                }
                MSP.promise(MSPCodes.MSP_SET_OSD_CONFIG, OSD.msp.encodeLayout(field)).then(function() {
                  updateOsdView();
                });
              }));
              osd_profile++;
            }
            _finalFieldName = titleizeField(_field3);
            $field.append('<label for="' + _field3.name + '" class="char-label">' + _finalFieldName + '</label>');
            if (_field3.positionable && _field3.isVisible[OSD.getCurrentPreviewProfile()]) {
              $field.append($('<input type="number" class="' + _field3.index + ' position"></input>').data('field', _field3).val(_field3.position).change($.debounce(250, function(e) {
                var field, position;
                field = $(this).data('field');
                position = parseInt($(this).val());
                field.position = position;
                MSP.promise(MSPCodes.MSP_SET_OSD_CONFIG, OSD.msp.encodeLayout(field)).then(function() {
                  updateOsdView();
                });
              })));
            }
            insertOrdered($displayFields, $field);
          }
        } catch (error1) {
          err = error1;
          _iterator5.e(err);
        } finally {
          _iterator5.f();
        }
        GUI.switchery();
        OSD.data.preview = [];
        OSD.data.display_size.total = OSD.data.display_size.x * OSD.data.display_size.y;
        _iterator6 = _createForOfIteratorHelper(OSD.data.display_items);
        _step6 = void 0;
        try {
          _iterator6.s();
          while (!(_step6 = _iterator6.n()).done) {
            _field4 = _step6.value;
            if (_field4.position > OSD.data.display_size.total) {
              _field4.position = 0;
            }
          }
        } catch (error1) {
          err = error1;
          _iterator6.e(err);
        } finally {
          _iterator6.f();
        }
        i = 0;
        while (i < OSD.data.display_size.total) {
          OSD.data.preview.push([null, ' '.charCodeAt(0), null, null]);
          i++;
        }
        _iterator7 = _createForOfIteratorHelper(OSD.data.display_items);
        _step7 = void 0;
        try {
          _iterator7.s();
          while (!(_step7 = _iterator7.n()).done) {
            _field5 = _step7.value;
            if (!_field5.preview || !_field5.isVisible[OSD.getCurrentPreviewProfile()]) {
              continue;
            }
            selectedPosition = _field5.position >= 0 ? _field5.position : _field5.position + OSD.data.display_size.total;
            _field5.preview_img = new Image;
            canvas = document.createElement('canvas');
            ctx = canvas.getContext('2d');
            if (_field5.preview.constructor !== Array) {
              i = 0;
              while (i < _field5.preview.length) {
                charCode = _field5.preview.charCodeAt(i);
                OSD.drawByOrder(selectedPosition++, _field5, charCode, i, 1);
                if (_field5.positionable) {
                  img = new Image;
                  img.src = FONT.draw(charCode);
                  ctx.drawImage(img, i * 12, 0);
                }
                i++;
              }
            } else {
              arrayElements = _field5.preview;
              i = 0;
              while (i < arrayElements.length) {
                element = arrayElements[i];
                if (element.constructor === String) {
                  j = 0;
                  while (j < element.length) {
                    charCode = element.charCodeAt(j);
                    OSD.drawByOrder(selectedPosition++, _field5, charCode, j, i);
                    if (_field5.positionable) {
                      img = new Image;
                      img.src = FONT.draw(charCode);
                      ctx.drawImage(img, j * 12, i * 18);
                    }
                    j++;
                  }
                  selectedPosition = selectedPosition - element.length + FONT.constants.SIZES.LINE;
                } else {
                  limits = OSD.searchLimitsElement(arrayElements);
                  offsetX = 0;
                  offsetY = 0;
                  if (limits.minX < 0) {
                    offsetX = -limits.minX;
                  }
                  if (limits.minY < 0) {
                    offsetY = -limits.minY;
                  }
                  charCode = element.sym;
                  OSD.drawByOrder(selectedPosition + element.x + element.y * FONT.constants.SIZES.LINE, _field5, charCode, element.x, element.y);
                  if (_field5.positionable) {
                    img = new Image;
                    img.src = FONT.draw(charCode);
                    ctx.drawImage(img, (element.x + offsetX) * 12, (element.y + offsetY) * 18);
                  }
                }
                i++;
              }
            }
            _field5.preview_img.src = canvas.toDataURL('image/png');
            _field5.preview_img.style.pointerEvents = 'none';
          }
        } catch (error1) {
          err = error1;
          _iterator7.e(err);
        } finally {
          _iterator7.f();
        }
        $preview = $('.display-layout .preview').empty();
        $row = $('<div class="row"/>');
        i = 0;
        while (i < OSD.data.display_size.total) {
          charCode = OSD.data.preview[i];
          if (_typeof(charCode) === 'object') {
            field = OSD.data.preview[i][0];
            charCode = OSD.data.preview[i][1];
            x = OSD.data.preview[i][2];
            y = OSD.data.preview[i][3];
          }
          $img = $('<div class="char" draggable><img src=' + FONT.draw(charCode) + '></img></div>').on('mouseenter', OSD.GUI.preview.onMouseEnter).on('mouseleave', OSD.GUI.preview.onMouseLeave).on('dragover', OSD.GUI.preview.onDragOver).on('dragleave', OSD.GUI.preview.onDragLeave).on('drop', OSD.GUI.preview.onDrop).data('field', field).data('position', i);
          $img.find('img').css('pointer-events', 'none');
          $img.attr('data-x', x).attr('data-y', y);
          if (field && field.positionable) {
            $img.addClass('field-' + field.index).data('field', field).prop('draggable', true).on('dragstart', OSD.GUI.preview.onDragStart);
          } else {

          }
          $row.append($img);
          if (++i % OSD.data.display_size.x === 0) {
            $preview.append($row);
            $row = $('<div class="row"/>');
          }
        }
        _iterator8 = _createForOfIteratorHelper(OSD.data.tooltips);
        _step8 = void 0;
        try {
          _iterator8.s();
          while (!(_step8 = _iterator8.n()).done) {
            tt = _step8.value;
            tt.destroy();
          }
        } catch (error1) {
          err = error1;
          _iterator8.e(err);
        } finally {
          _iterator8.f();
        }
        OSD.data.tooltips = [];
        $('.osd_tip').each(function() {
          OSD.data.tooltips.push($(this).jBox('Tooltip', {
            delayOpen: 100,
            delayClose: 100,
            position: {
              x: 'right',
              y: 'center'
            },
            outside: 'x'
          }));
        });
      });
    };
    SYM.loadSymbols();
    OSD.loadDisplayFields();
    fontPresetsElement = $('.fontpresets');
    OSD.constants.FONT_TYPES.forEach(function(e, i) {
      var option;
      option = $('<option>', {
        'data-font-file': e.file,
        value: e.file,
        text: e.name
      });
      fontPresetsElement.append($(option));
    });
    fontbuttons = $('.fontpresets_wrapper');
    fontbuttons.append($('<button>', {
      "class": 'load_font_file',
      i18n: 'osdSetupOpenFont'
    }));
    LogoManager.init(FONT, SYM.LOGO);
    i18n.localizePage();
    OSD.GUI.fontManager = new jBox('Modal', {
      width: 750,
      height: 455,
      closeButton: 'title',
      animation: false,
      attach: $('#fontmanager'),
      title: 'OSD Font Manager',
      content: $('#fontmanagercontent')
    });
    $('.elements-container div.cf_tip').attr('title', i18n.getMessage('osdSectionHelpElements'));
    $('.videomode-container div.cf_tip').attr('title', i18n.getMessage('osdSectionHelpVideoMode'));
    $('.units-container div.cf_tip').attr('title', i18n.getMessage('osdSectionHelpUnits'));
    $('.timers-container div.cf_tip').attr('title', i18n.getMessage('osdSectionHelpTimers'));
    $('.alarms-container div.cf_tip').attr('title', i18n.getMessage('osdSectionHelpAlarms'));
    $('.stats-container div.cf_tip').attr('title', i18n.getMessage('osdSectionHelpStats'));
    $('.warnings-container div.cf_tip').attr('title', i18n.getMessage('osdSectionHelpWarnings'));
    $('.osdprofile-selector').change(updateOsdView);
    $('.osdprofile-active').change(function() {
      OSD.data.osd_profiles.selected = parseInt($(this).val());
      MSP.promise(MSPCodes.MSP_SET_OSD_CONFIG, OSD.msp.encodeOther()).then(function() {
        updateOsdView();
      });
    });
    $('a.save').click(function() {
      var self;
      var oldText;
      self = this;
      MSP.promise(MSPCodes.MSP_EEPROM_WRITE);
      GUI.log(i18n.getMessage('osdSettingsSaved'));
      oldText = $(this).text();
      $(this).html(i18n.getMessage('osdButtonSaved'));
      setTimeout((function() {
        $(self).html(oldText);
      }), 2000);
    });
    fontPreviewElement = $('.font-preview');
    FONT.initData();
    fontPresetsElement.change(function(e) {
      var $font, fontver;
      $font = $('.fontpresets option:selected');
      fontver = 1;
      if (semver.gte(CONFIG.apiVersion, '1.42.0')) {
        fontver = 2;
      }
      $('.font-manager-version-info').text(i18n.getMessage('osdDescribeFontVersion' + fontver));
      $.get('./resources/osd/' + fontver + '/' + $font.data('font-file') + '.mcm', function(data) {
        FONT.parseMCMFontFile(data);
        FONT.preview(fontPreviewElement);
        LogoManager.drawPreview();
        updateOsdView();
        $('.fontpresets option[value=-1]').hide();
      });
    });
    fontPresetsElement.change();
    $('button.load_font_file').click(function() {
      FONT.openFontFile().then(function() {
        FONT.preview(fontPreviewElement);
        LogoManager.drawPreview();
        updateOsdView();
        $('.font-manager-version-info').text(i18n.getMessage('osdDescribeFontVersionCUSTOM'));
        $('.fontpresets option[value=-1]').show();
        $('.fontpresets').val(-1);
      })["catch"](function(error) {
        return console.error(error);
      });
    });
    $('a.flash_font').click(function() {
      if (!GUI.connect_lock) {
        $('a.flash_font').addClass('disabled');
        $('.progressLabel').text(i18n.getMessage('osdSetupUploadingFont'));
        FONT.upload($('.progress').val(0)).then(function() {
          $('.progressLabel').text(i18n.getMessage('osdSetupUploadingFontEnd', {
            length: FONT.data.characters.length
          }));
        });
      }
    });
    $('a.replace_logo').click(function() {
      if (GUI.connect_lock) {
        return;
      }
      LogoManager.openImage().then(function(ctx) {
        LogoManager.replaceLogoInFont(ctx);
        LogoManager.drawPreview();
        LogoManager.showUploadHint();
      })["catch"](function(error) {
        return console.error(error);
      });
    });
    $(document).on('click', 'span.progressLabel a.save_font', function() {
      chrome.fileSystem.chooseEntry({
        type: 'saveFile',
        suggestedName: 'baseflight',
        accepts: [
          {
            description: 'MCM files',
            extensions: ['mcm']
          }
        ]
      }, function(fileEntry) {
        if (chrome.runtime.lastError) {
          console.error(chrome.runtime.lastError.message);
          return;
        }
        chrome.fileSystem.getDisplayPath(fileEntry, function(path) {
          console.log('Saving firmware to: ' + path);
          chrome.fileSystem.isWritableEntry(fileEntry, function(isWritable) {
            var blob;
            if (isWritable) {
              blob = new Blob([intel_hex], {
                type: 'text/plain'
              });
              fileEntry.createWriter((function(writer) {
                var truncated;
                truncated = false;
                writer.onerror = function(e) {
                  console.error(e);
                };
                writer.onwriteend = function() {
                  if (!truncated) {
                    truncated = true;
                    writer.truncate(blob.size);
                    return;
                  }
                };
                writer.write(blob);
              }), function(e) {
                console.error(e);
              });
            } else {
              console.log('You don\'t have write permissions for this file, sorry.');
              GUI.log(i18n.getMessage('osdWritePermissions'));
            }
          });
        });
      });
    });
    $(document).keypress(function(e) {
      if (e.which === 13) {
        $('a.flash_font').click();
      }
    });
    GUI.content_ready(callback);
  });
};

TABS.osd.cleanup = function(callback) {
  PortHandler.flush_callbacks();
  if (OSD.GUI.fontManager) {
    OSD.GUI.fontManager.destroy();
  }
  $(document).unbind('keypress');
  $(document).off('click', 'span.progressLabel a');
  if (callback) {
    callback();
  }
};
